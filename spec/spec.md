# WentianCode（文天）Spec（现行版本：v0.13）

## 背景

yuning 要从零做一个自己的命令行 AI 助手（类 Claude Code），作为长期演进的 coding agent 的起点。v0.1 已完成最小内核：终端里的多轮流式对话，打通「多后端 + 流式输出 + extended thinking + Markdown 渲染 + 会话持久化」主干（F1–F12，全部验收）。v0.2 已完成 Claude Code 式交互体验（横幅 / 后端选择 / 多行输入 / 状态栏 / 计时反馈 / 中断，F13–F18，全部验收）。

**v0.3 范围**：工具系统——助手从「只能动嘴」变成「能干活」。模型在回复中可请求调用工具（读文件、写文件、改文件、执行命令、按模式找文件、搜文件内容），程序执行后把结果回灌给模型，模型据此给出最终回答（F19–F28）。本版只做**单轮工具执行**：一次回复内的并列工具调用全部执行并一并回灌，模型的后续回复若再请求工具则不执行；多轮自动循环（Agent Loop）留给 v0.4。（v0.3 已验收，2026-06-11 离线项全绿。）

**v0.4 范围**：Agent Loop——助手从「一步一停」变成「自主干活」。模型拿到工具结果后可继续请求工具，循环往复直到任务完成（F29–F34）。配套四件事：循环过程以统一事件流输出（界面与循环逻辑彻底解耦）；流式双路（实时显示与完整累积并行）；并列工具调用按安全性分批（只读并发、副作用串行）；计划模式两段式（`/plan` 只读勘察出计划、`/do` 切回全工具执行）。权限系统、上下文压缩留给后续版本。

**v0.5 范围**：结构化系统提示 + 提示词缓存——助手从「能干活」变成「干得好」。把原来「工具清单 + cwd」三五行的裸 `system` 字符串，展开成**七模块结构化系统提示**（身份 / 系统约束 / 任务模式 / 动作执行 / 工具使用 / 语气风格 / 文本输出），按优先级有序拼装、模块可插拔（F35）；身份/语气/输出三模块写出**文天人格**（沙雕、幽默、真诚、爱照顾朋友的命令行编程伙伴，油塌头 `=^_^=`，底子是严谨工程师）（F36）；关键工具约定在系统提示与工具描述里**双重强化**（F37）。区分稳定与动态：稳定的「工具声明 + system」前缀走**可缓存通道**（Anthropic 显式 `cache_control` 断点，OpenAI 兼容走自动前缀缓存）（F38）；环境信息与会话级开关提醒走**消息通道**，以 `<system-reminder>` 标签在请求时注入、永不持久化，开关提醒按轮次注入（首轮完整 / 间隔重复 / 其余精简）（F39）；解析后端返回的缓存命中字段并入用量、可观测验证（F40）。项目指令文件加载、自动记忆、真实 MCP 接入、自动化提示评估留给后续版本（仅预留可选模块槽位）。

**v0.6 范围**：权限系统——助手从「会自己干」变成「在安全护栏内自己干」。此前工具执行链路完全裸奔：写文件能写任意路径、改文件能改任意文件、执行命令能跑任意命令（含 `rm -rf /`）、读写类工具能触碰项目目录之外的任何位置。本版给文天装上**五层防御**：危险命令黑名单硬拦截（F41）、路径沙箱限定操作范围（F42）、可配置规则做细粒度放行/拦截（F43）、三级配置加载合并（F44）、多档权限模式按工具类别兜底（F45）；五层串联、逐层短路、判定只有 Allow/Deny/Ask 三态（F46），模式可在运行时 Shift+Tab 切换且跨轮保持（F47）；规则未明确命中且模式判为 Ask 时落到人在回路三选一（F48）。任一层的 Deny 都包成**结构化拒绝结果回灌**给模型、Agent Loop 不中断（F49）。权限判定发生在 agent 编排层、与 provider 无关。网络限制、资源配额、审计日志留给后续版本。

**v0.7 范围**：MCP 客户端接入——助手从「只会用内置的六个工具」变成「能挂载任意外部 MCP Server 的工具」。启动时按配置发现外部 MCP Server，通过标准 MCP 协议（JSON-RPC 2.0）完成握手、列出其工具、按需调用，并把发现到的远端工具**适配成现有统一工具抽象**注册进工具中心，与六个内置工具并列；模型/Agent Loop 多轮循环调用时完全无感（F50–F55）。配置上把 provider 与 MCP Server 列表都改为**用户级 + 项目级两层合并**读取（F50）；协议会话三步走（初始化握手 / 列工具 / 调用），JSON-RPC 请求带 id、响应按 id 配对（F51）；支持两种传输——本地子进程走 stdio 管道、远程走 Streamable HTTP（F52）；远端工具包装为统一工具、命名空间隔离、结果回灌、错误结构化（F53）；外部工具默认按「有副作用」对待、仅 Server 标注 readOnlyHint 才降只读（F54）；多 Server 连接缓存与生命周期管理、单个 Server 故障只告警跳过不影响其余、退出时统一关闭（F55）。本版只接入 MCP 的**工具能力**，不做资源/提示词/采样，不做 Server 健康检查与自动重连，不做 MCP 工具的细粒度权限规则（仅经「默认有副作用」标志接入既有确认/兜底）。

**v0.8 范围**：上下文管理 + 双层压缩——助手从「能长时间干活但会被上下文撑爆」变成「在有限 token 预算里长时间干活不瘫」。token 大头是工具结果，压缩从这里下手、用户原始消息尽量原文保留（F56–F62）。两层策略：**第一层预防**（轻量）——单个工具结果超阈值就存盘、对话里只留预览与文件路径，单轮工具结果合计超阈值则挑大的依次存盘（F57）；**第二层兜底**（重量）——整体对话逼近窗口上限时调 LLM 生成结构化摘要，把较早消息摘要掉、近期原文留下（从尾部按 token 往回数约 1 万 token 或至少 5 条），摘要按八个固定部分组织（F58），摘要 Prompt 禁止调用任何工具并要求先草稿后正式、草稿用完即弃（F59），压缩后补一条边界消息提示重新读取文件、勿照摘要脑补代码（F60）。token 用近似估算（锚定上次 API usage + 增量按字符折算，不上精确 tokenizer），两家后端 `input_tokens` 语义差异在 provider 层消化（F56）。压缩在 Agent Loop **每轮调用后端之前**触发（即「每次 API 请求前」）：先跑预防、再判兜底；自动触发留 13K 安全余量防估算误差、用户 `/compact` 手动触发收窄到 3K；摘要连续失败 3 次熔断（F61/F62）。本版不上精确 tokenizer、不做摘要策略的机器学习优化、不做跨会话记忆提炼。

**v0.9 范围**：记忆与会话——助手从「每次失忆」变成「越用越懂你」。三套机制分别填回 v0.5 留下的两个空槽并重构会话层（F63–F69）：**项目指令文件**——启动时从「项目本地覆盖 `<cwd>/.wentian/WENTIAN.md` > 项目根 `<cwd>/WENTIAN.md` > 用户全局 `~/.config/wentian/WENTIAN.md`」三层读手写 Markdown 指令、按优先级高在前拼接注入系统提示的「项目/自定义指令」模块（v0.5 起恒空、本版起真渲染），支持独占一行的 `@include` 内联展开（限嵌套深度、`visited` 防环、先解析符号链接再前缀比对拦越界、总体积上限截断）（F63）；**会话存档改 JSONL + 按 cwd 分区**——从「单文件全量 JSON 重写」改为每会话一个 JSONL、按消息追加写（崩溃只丢半行），存到 `~/.local/share/wentian/projects/<cwd-slug>/sessions/<id>.jsonl`、不再维护独立 meta 文件，`/sessions` 与 `--continue` 默认只看当前项目分区、`--all` 看全部（F64）；**会话恢复卫生**——`load`/`resume` 做四道处理：坏行跳过、尾部未配对 tool_call 截断、溢出复用 v0.8 `Compactor` 先压一次、距上次 `updated_at` 超阈值经 v0.5 `<system-reminder>` 通道注入一次性时间跨度提醒（绝不写回持久化）（F65），并惰性清理超 `retention_days`（默认 30 天）的过期会话及其 `.artifacts/`（F66）；**自动记忆**——每个 `COMPLETED` 回合后启 daemon 线程 fire-and-forget 抽取（不阻塞输入、异常静默吞 stderr、自建 provider 实例不跨线程共享），调 LLM 产出**用户偏好 / 纠正反馈 / 项目知识 / 参考资料**四类带 frontmatter 的 Markdown 笔记、去重交 LLM 判断（F67），笔记按用户级 `~/.config/wentian/memory/` 与项目级 `<cwd>/.wentian/memory/` 分级存、每域一份 `INDEX.md` 摘要索引，build_app 时读两份索引注入「长期记忆」模块（v0.5 起恒空、本版起真渲染，启动注入一次不热刷、本会话新记忆下一会话生效）（F68）；配置沿用 v0.7 两层深合并新增全可选带默认的 `memory:` / `sessions:` 块（F69）。本版不做向量库 / 嵌入检索、不做 RAG 式按需召回、不做跨机器记忆同步、不做会话内记忆热刷新、不做精确 tokenizer、不做指令文件热重载——并由此**作废** v0.5「不做项目指令文件加载」「不做自动记忆 / 长期记忆提炼」两条。

**v0.10 范围**：斜杠命令系统——把交互层的「命令」从 REPL 里临时手写的分发器收口成一套**注册中心 + 解析 + 分发**机制，让用户用 `/` 开头的输入绕过 Agent、直接执行本地操作或触发预设提示词（F70–F76）。常用操作（清屏、查状态、切模式、清空上下文）响应快、省 token、行为确定，不让 LLM 处理这类非对话杂事。**命令注册中心**管元数据（名称 / 别名 / 简短描述 / 用法示例 / 类型 / 参数提示 / 是否隐藏 / 处理函数），启动期检测命名或别名冲突即 panic 退出、不延后到运行时（镜像既有 `ToolRegistry` 重名 `raise` 模式）（F70）；**解析器**识别 `/` 前缀、第一个空格前为命令名、之后为参数、命令名转小写大小写不敏感，空输入早返回、未命中带 `/help` 引导（F71）；命令按执行模式分**三类**——纯本地 / 影响界面状态 / 把预设提示词送进对话交给 AI（F72）；抽一层**界面控制接口**（显示消息 / 发送用户消息 / 切换模式 / 查 token / 刷状态等）让命令实现不绑定具体渲染框架，状态栏联动模式标记 `[DEFAULT]`/`[PLAN]`（F73）；在用户回车入口加**分流器**，斜杠走本地分发、否则送 AI（F74）；支持**别名与 Tab 补全**，单匹配直接补、多匹配弹菜单、隐藏命令不参与（F75）；内置**十个高频命令** `/help`、`/clear`、`/compact`、`/plan`、`/do`、`/session`、`/memory`、`/permission`、`/status`、`/review`（模式切换拆成 `/plan` 进、`/do` 退两个动词），并把 v0.1 起临时手写的 `/new`/`/sessions`/`/resume`/`/provider`/`/exit` 一并归并进注册中心（`/new`/`/sessions`/`/resume` 折成 `/session` 子命令，`/provider`/`/exit` 独立登记），单一分发路径无旁路（F76）。本版不做用户自定义命令、不做动态生成提示词、不做命令级权限控制——留给后面的 Skill 系统。

**v0.11 范围**：Skill 系统——让用户把可复用的 AI 操作封装成独立的带元信息 Markdown 文件，不必反复输入同样的提示词（F84–F90）。单个 Skill = YAML frontmatter（`name`/`description`/`allowed_tools`/`mode`/`history`/`model`）+ Markdown 正文（SOP 指令，支持 `$ARGUMENTS`/`$1` 占位符替换），单文件或目录型两种形态（F84）；从**项目 > 用户 > 内置**三层目录发现、同名高层覆盖、单文件解析失败跳过不阻断（F85）。**两阶段加载**：启动只把 `name+description` 注入系统提示一个稳定的「可用 Skill」菜单（复用 v0.5 预留的「已激活 Skill」槽、不破缓存前缀），要用时模型调一个**系统级、不受白名单约束**的 `load_skill(name,args)` 工具加载完整正文（F86）。**共享模式**激活后：完整正文每轮经 v0.8 `request_decorator` 走 `<system-reminder>` 钉在最新用户消息处（重建不持久化、多 Skill 并存），并复用 v0.4/v0.6 `allowed_tools` 把可用工具收窄到各激活 Skill 白名单的并集 + 恒含 `load_skill`（任一 Skill 不声明白名单则不收窄）（F87）；启动时校验每个 Skill 白名单引用的工具都存在（**排在 MCP 发现之后**），引用错工具即 `raise` fail-fast、区别于解析失败的静默跳过（F88）。**独立模式**激活则当场在 worker 线程跑一个嵌套 AgentLoop 子对话（带 `history` 条主历史、子系统提示拼正文、子工具=白名单、可指定模型），把子对话最终助手正文作为 `load_skill` 工具结果回流主历史（F89）。每个 Skill 自动注册成 v0.10 注册中心里的 PROMPT 命令 `/<name>`（与 `load_skill` 等价、进 `/help` 与 Tab 补全），同名冲突按「替换提示词类命令 / 保护控制类命令跳过告警」消解；另有 `/skills`（列举）、`/skills reload`（重扫热更新、reload 校验失败保旧不崩）、`/clear`/`/session new` 清空激活集；随包内置 `commit`/`review`/`test` 三样板（F90）。本版**不做**专属工具脚本的加载执行（目录型 `tools/` 仅识别结构）、不做 Skill 市场分发 / 版本管理、不做 Skill 实时热重载 / 向量召回 / Skill 级权限 / 嵌套激活——专属工具执行与分发/版本留给后续章节。

## 目标

- 在终端启动后进入交互式对话界面，输入问题，流式逐字看到回复
- 助手回复以 Markdown 渲染呈现（标题、列表、代码块等有格式区分），不是裸源码
- 支持多轮对话，助手记得之前说过的话
- 一套配置切换多家大模型后端（Claude / OpenAI 协议 / 国产兼容模型）
- 支持 Claude 的 extended thinking，思考过程可见
- 后端通过统一接口抽象，方便以后加新后端
- 对话跨会话持久化，下次能接着聊
- 模型能调用本地工具读写文件、执行命令、搜索代码，从聊天机器人变成能干活的助手（v0.3）
- 模型能围绕一个任务自主多轮调用工具直到完成——过程可见、可中断、有安全刹车（v0.4）
- 系统提示结构化、可插拔、有人格，稳定部分走缓存省钱省时延、动态部分走消息通道不污染缓存，让助手从「能干活」变「干得好」（v0.5）
- 工具执行前必须穿过五层权限护栏（黑名单 / 沙箱 / 规则 / 模式 / 人在回路），危险命令拦得住、文件操作出不了项目根、规则可配、信任等级可切换、关键时刻交回用户确认；被拒不终止循环而是回灌让模型调整策略（v0.6）
- 在配置里声明若干外部 MCP Server，启动时自动发现并把它们的工具接进工具中心，文天像用内置工具一样无感调用远端工具；本地 Server 走子进程、远程 Server 走 HTTP，单个 Server 挂了不拖垮其余（v0.7）
- 在有限 token 预算里长时间干活也不被上下文撑爆：超大工具结果自动存盘、对话里只留预览与路径；对话逼近窗口上限时自动调 LLM 把较早历史摘要成结构化几段、近期原文保留；用户可手动触发压缩、摘要失败能熔断（v0.8）
- 从「每次失忆」变「越用越懂你」：启动时读三层 `WENTIAN.md` 项目指令（支持 `@include`）注入系统提示；会话改 JSONL 追加写、按 cwd 分区存储，恢复时坏行跳过 / 未配对截断 / 溢出先压一次 / 跨时间提醒、过期会话惰性清理；每轮对话后后台抽取四类长期记忆（用户偏好 / 纠正反馈 / 项目知识 / 参考资料）分级落盘、下次启动注入「长期记忆」模块——填回 v0.5 留的两个空槽（v0.9）
- 斜杠命令收口成注册中心 + 解析 + 分发：`/` 开头的输入绕过 Agent 直接本地执行或触发预设提示词，常用杂事（清屏 / 查状态 / 切模式 / 清空上下文）响应快、省 token、行为确定；命令带别名与 Tab 补全、按本地 / 界面状态 / 提示词三类执行、经一层界面控制接口与渲染框架解耦；启动期别名冲突即 panic（v0.10）

## 功能需求

- F1: 启动后进入滚动式交互对话界面，显示提示符并等待用户输入
- F2: 用户输入一行问题并提交后，调用当前后端的大模型 API
- F3: 模型回复以流式方式逐字/逐块实时显示，而非等全部生成完再显示
- F4: 维护多轮对话上下文，每次请求携带历史对话，使助手记得之前内容
- F5: 支持两种后端协议——Anthropic 协议、OpenAI 兼容协议（后者覆盖国产兼容模型）
- F6: 通过 YAML 配置文件管理多个后端，每个后端含 protocol / model / base_url / api_key 四个核心字段，并指定一个默认后端
- F7: 启动时可临时指定使用哪个后端，覆盖默认
- F8: 对 Anthropic 后端支持 extended thinking，思考内容与正文回复在界面上可区分地展示
- F9: 每轮对话结束后自动把会话持久化到本地；支持下次启动续上一次会话，也支持开启全新会话
- F10: 提供会话内命令：查看帮助、新建会话、列出/恢复历史会话、切换后端、退出
- F11: 后端以统一接口抽象，新增一种后端不需要改动界面层与会话层
- F12: 助手正文回复以 Markdown 渲染显示（至少支持标题、列表、代码块、行内代码、粗/斜体）；渲染与流式不冲突——生成过程中内容持续逐步出现，最终定格为渲染后的格式化文本，而非裸 Markdown 源码。思考内容（F8）保持纯文本展示，不做 Markdown 渲染

以下为 v0.2 新增（交互体验对齐 Claude Code）：

- F13: 启动时显示欢迎横幅，布局对齐 Claude Code：左侧 icon 即文本猫脸 `=^_^=` 本体（朱砂色加粗；不做像素画，任何终端字体下都不变形）；右侧为信息行——程序名与版本号、当前后端与模型、会话状态（新会话或已恢复及其标识）。不使用整框包裹
- F14: 配置了多个后端且启动时未显式指定后端时，进入对话前展示可用后端列表，用方向键选择、回车确认（默认后端预先高亮）；取消选择则使用默认后端。只配置一个后端或已显式指定时不出现此列表
- F15: 输入区为多行输入框，提示符为 `❯ `（输入区不绘制边框：边框需要在 prompt_toolkit 渲染器之外写终端，会与重绘机制冲突）——回车提交；提供专用按键换行；上下方向键可翻阅历史输入，历史跨程序重启保留
- F16: 输入框下方常驻一行状态栏，显示当前后端与模型、会话标识、当前消息数；会话内切换后端/新建/恢复会话后，状态栏内容随之更新
- F17: 从提交问题到回复结束期间提供持续的活动反馈：以单行文本猫脸帧（`=^_^=` 与 `=-_-=` 眨眼轮换）加已用秒数显示，等待期与正文流式期一致（流式期随正文一同呈现），回复定格后计时信息消失
- F18: 生成期间可随时中断本轮回复（专用按键或中断快捷键），中断不退出程序、不破坏会话：已产生部分正文时，部分正文保留在屏幕与对话历史中，并有可见的「已中断」提示；尚无任何正文时，本轮提问从历史中撤销（不留未回答的提问）。中断后可立即继续下一轮输入

以下为 v0.3 新增（工具系统）：

- F19: 工具以统一抽象定义：每个工具具有名称、面向模型的功能描述、参数结构描述（各参数的名称/类型/是否必填/说明）和执行入口。新增一个工具不需要改动对话回合逻辑与界面层
- F20: 内置六个核心工具：
  - 读文件：按路径读取文本文件内容，支持只读取指定行范围
  - 写文件：将完整内容写入指定路径（不存在则创建，必要时创建父目录；存在则覆盖）
  - 改文件：在指定文件中做「原文精确替换」——给出原文片段与替换片段，原文必须在文件中**唯一**出现
  - 执行命令：在工作目录下执行一条 shell 命令，返回输出与退出码
  - 找文件：按通配模式（如 `**/*.py`）列出匹配的文件路径
  - 搜内容：按正则在文件内容中搜索，返回匹配的文件、行号与行内容
- F21: 提供工具注册中心：集中登记全部工具，支持按名称查找，并能生成传给后端 API 的工具声明列表。模型调用未注册的工具名时，返回结构化错误结果而非崩溃
- F22: 两种后端协议（Anthropic、OpenAI 兼容）都支持工具调用全链路：把工具声明随请求发出；解析模型流式回复中的工具调用请求（含分片到达的参数碎片拼接）；把工具执行结果以协议要求的格式回传给模型
- F23: 单轮工具执行语义：模型一次回复中发出的全部并列工具调用依次执行，结果一并回灌，模型据此生成后续回复；若后续回复中再次请求工具，则不执行，界面提示本版仅支持单轮工具调用，已生成的文字正常保留，本轮结束回到输入状态。不含工具调用的普通回复行为与 v0.2 完全一致
- F24: 工具执行的健壮性：每次执行有超时上限；任何失败（参数缺失或非法、文件不存在、命令出错、超时、未注册工具）都包装成结构化错误结果回灌模型，使其能调整重试或向用户说明——程序不崩溃、会话不中断
- F25: 改文件工具的精确语义：原文片段在文件中恰好出现一次才执行替换；出现零次或多次时不改动文件，返回包含实际匹配次数的明确错误，让模型能修正后重试
- F26: 副作用工具（写文件、改文件、执行命令）执行前展示将要做的事（目标文件或命令内容），等待用户确认；用户拒绝时该工具不执行，以「用户拒绝执行」作为结果回灌模型。只读工具（读文件、找文件、搜内容）直接执行不询问
- F27: 工具调用过程在界面可见：每次调用显示工具名与关键参数摘要、执行结果状态（成功/失败/被拒绝）与结果摘要，样式与正文回复可区分
- F28: 工具调用与其结果作为会话历史的一部分持久化；恢复会话后模型仍能引用之前工具调用的结果，上下文完整不丢

以下为 v0.4 新增（Agent Loop）：

- F29: 多轮自动工具循环（ReAct 模式）：模型回复中请求工具 → 程序执行并回灌结果 → 模型据此继续（可再请求工具），循环往复直到任务完成。五个停机条件全部具备，且停机原因在界面上可区分：
  - 模型自然完成：回复中不再请求工具
  - 轮数上限（兜底安全网）：单回合循环轮数有可配置上限；达上限时剩余工具请求不执行、已生成文字保留、界面明确提示
  - 用户中断：模型生成期间可随时中断；语义沿 v0.2/v0.3——已有文字保留入史，本轮未执行的工具请求丢弃，已完成轮次的工具交互保留；历史任何时刻都保持「工具请求与结果成对」
  - 未知工具空转：连续多轮的全部调用都是未注册工具名时停止循环（防幻觉死循环）；其间出现任何已注册调用则计数重置
  - 流式出错：当前轮丢弃、已完成轮次保留，界面显示错误，程序不崩溃、会话可继续
- F30: 循环过程以统一事件流对外输出：轮次开始/结束、思考与正文增量、工具调用发起、工具结果、token 用量、循环结束（含停机原因）。界面层仅消费事件流做渲染，不参与循环决策；更换或扩展界面无需改动循环逻辑（可测性：用「事件记录器」替代界面即可完整断言一次循环）
- F31: 流式双路收集：正文增量实时推送界面（边生成边显示，观感与 v0.2 一致），同时完整累积本轮回复（文本、全部工具调用、协议原始块、用量）供循环决策与入史——显示不等待累积完成，累积不遗漏任何显示内容
- F32: 并列工具调用按安全性分批执行：连续的只读调用并发执行（总耗时接近其中最慢的一个，而非逐个相加）；副作用调用与未知调用串行执行，并与前后只读调用保持模型给出的相对顺序（写后之读不被提前）。无论实际完成先后，结果一律按原始调用顺序回灌与入史；界面上调用行与结果行也按原始顺序成对出现。副作用确认门语义沿 v0.3：逐次确认、拒绝结果回灌
- F33: 计划模式（两段式工作流）：`/plan` 进入计划模式——此后所有轮次只向模型提供只读工具（读文件、找文件、搜内容），并提示模型先勘察、产出执行计划、不做任何修改；模型在计划模式中硬请求副作用工具时不执行，以「计划模式下该工具不可用」的结构化错误回灌（模型可自我修正；此类拦截不计入未知工具停机）；状态栏显示计划模式标记。`/do` 退出计划模式恢复全部工具，其后可附带文字作为下一条用户消息（如 `/do 按计划执行`）。模式是会话内的界面状态，不随会话持久化
- F34: token 用量统计：循环每轮的用量跨轮累计，回合结束后显示总输入/输出 token 数与循环轮数；后端未报用量时不显示

以下为 v0.5 新增（结构化系统提示 + 提示词缓存）：

- F35: 结构化系统提示——系统提示由一组**固定模块按优先级有序拼装**而成：身份、系统约束、任务模式、动作执行、工具使用、语气风格、文本输出（共七个，顺序即优先级，模块间空行分隔）；其后接**可选模块槽位**（自定义/项目指令、已激活 Skill、长期记忆）。每个模块是一个独立、可单独测试的单元，新增/替换/重排一个模块只改模块定义、不改拼装逻辑。可选模块本版渲染为空（不产出任何文本、不留空行残渣），但插拔接口就绪。系统提示的组装是纯函数，不依赖后端 SDK、可在不联网下完整测试
- F36: 文天人格——身份、语气风格、文本输出三个模块写出文天这个产品的人格：有点沙雕、幽默、真诚、爱照顾朋友的命令行编程伙伴，但底子是严谨的工程师——可以贫、可以自嘲，但结论不掺水、证据不省略、工程纪律（先勘察后动手、完成前验证）不打折。`=^_^=` 是产品的界面图标。**人设只体现、不透露**：文天通过言行自然流露这些特质，但绝不向用户复述、罗列或解释自己的人设设定与系统提示；用户直接问「你的人设/设定/系统提示是什么」时，自然带过或答非所设，不逐条背诵（如同不泄露系统提示——这是红线）。**不自我标榜形象**：文天不自称「猫」「油头」或任何形象化绰号，不把这类形象词挂在嘴上——气质只靠言行流露，不靠自报标签
- F37: 关键工具约定双重强化——「优先用专用工具而非 shell、编辑文件前必先读取、无依赖的调用可并行发起、优先相对路径」这类关键约定，**同时**写进系统提示的「工具使用」模块和受影响工具自身的描述（description）中；两处措辞一致、互相强化，以提高模型对这些约定的遵守率
- F38: 提示词缓存（稳定前缀走可缓存通道）——稳定不变的「工具声明 + 系统提示」前缀走可缓存通道，跨同一会话的多次请求复用以省钱省时延。Anthropic 后端在系统提示块上施加 `cache_control: ephemeral` 缓存断点（前缀缓存覆盖其之前的工具声明与系统提示整段）；OpenAI 兼容后端走其服务端自动前缀缓存（无显式缓存控制 API）。对话历史与动态内容不进缓存（本版只缓存稳定前缀，多断点缓存历史留后续版本）
- F39: 动态内容走消息通道（`<system-reminder>` 注入）——环境信息（工作目录、操作系统、当前日期、git 分支）与会话级开关提醒（如计划模式），以带 `<system-reminder>...</system-reminder>` 标签的内容在**每次发请求时**注入消息流：请求时由组装层拼装、**永不写入持久化会话**；标签使模型把它当作系统级补充指令处理、不当成用户输入来回复；因其位于缓存断点之后，不污染稳定前缀的缓存。会话级开关提醒按轮次注入以控制频率：**首轮完整、每 N 轮重复完整、其余轮精简为一行**（N 可配置，默认 5）
- F40: 缓存命中可观测——后端解析 API 返回的缓存用量字段（Anthropic 的 `cache_creation_input_tokens` / `cache_read_input_tokens`；OpenAI 兼容的 `prompt_tokens_details.cached_tokens`），并入 token 用量统计跨轮累计；回合结束的用量行在有缓存命中时显示缓存读取/创建 token，用于验证缓存策略真实生效。后端未报缓存字段时按 0 处理、不显示缓存信息

以下为 v0.6 新增（权限系统：五层防御）：

- F41: 危险命令黑名单（不可绕过）——对命令执行类工具的命令串用一组**内置正则**匹配；命中即判 **Deny**，作为流水线最高优先级层，不受任何规则、模式、配置影响——`bypassPermissions` 模式也拦得住。覆盖已知高危模式（如递归强删根/家目录、写块设备、fork 炸弹、重定向覆盖磁盘设备、格式化文件系统等）。黑名单是**启发式防御**而非完备保证，明确不追求穷尽所有危险命令；用户**不可增删、不可关闭**黑名单。
- F42: 路径沙箱——对带路径参数的文件类工具（读文件 / 写文件 / 改文件 / 找文件 / 搜内容）提取目标路径，规整为绝对路径并**先解析符号链接、再做前缀判断**，判断是否落在项目根目录内；逃出项目根即判 **Deny**。对尚不存在的目标（如新建文件、含未创建的中间目录）解析其**最近的已存在祖先目录**后再判断，不因目标不存在而误判。解析顺序固定，防止用软链接指向外部目录绕过。**命令执行（Bash）不走沙箱**（其危险由黑名单 + 规则 + 模式兜底覆盖；任意命令的文件访问无法可靠静态解析）。沙箱对读取与写入**一视同仁**地限制在项目内。
- F43: 规则引擎——规则以「工具名(模式)」声明，结果只有 **allow** 或 **deny** 两种。工具名用面向用户的友好名（Bash / Read / Write / Edit / Glob / Grep），由引擎映射到对应内置工具。模式段支持**精确匹配**（如 `Bash(git status)`）与 **glob 匹配**（如 `Bash(git *)`、`Write(src/**)`）；不带模式段（如 `Bash`）表示匹配该工具全部调用。`*` 匹配任意字符序列；`**` 的跨目录段语义**仅对文件路径有意义**，命令串不解释 `**`（等价于 `*`）。匹配对象：命令执行类比对命令串，文件类工具比对**项目相对路径**。对一次工具调用，引擎在给定规则集内判出 allow / deny / 未命中。
- F44: 多层配置加载与合并——从**用户级、项目级、本地级**三个配置文件读取 allow/deny 规则与可选的默认模式，构成三级规则集。优先级 **本地 > 项目 > 用户**，从高到低逐层判断、就近命中即止；**同一层内 deny 优先于 allow**。任一文件缺失视为空规则集；**配置文件格式错误绝不导致引擎构造失败**，只把该文件降级为空规则集（不额外放开任何权限），不崩溃。启动时的默认模式取自配置（本地 > 项目 > 用户），未配置则为 `default`。三层文件位置：用户级 `~/.config/wentian/settings.yaml`、项目级 `<项目根>/.wentian/settings.yaml`、本地级 `<项目根>/.wentian/settings.local.yaml`（本地级 gitignore，可含敏感放行规则）。
- F45: 权限模式与按类别兜底——工具分三类：**只读**（读文件 / 找文件 / 搜内容）、**文件写**（写文件 / 改文件）、**命令执行**（Bash）。四档模式给出**规则未命中时**的兜底裁决，**只产 Allow 或 Ask（绝不产 Deny）**：

  | 模式 | 只读 | 文件写 | 命令执行 |
  |---|---|---|---|
  | default | Allow | Ask | Ask |
  | acceptEdits | Allow | Allow | Ask |
  | plan | Allow | Ask | Ask |
  | bypassPermissions | Allow | Allow | Allow |

  plan 档额外沿用 v0.4 行为（F33）：仅向模型暴露只读工具定义 + 注入计划提醒，文件写 / 命令执行压根不会被请求（矩阵中 plan 行的 Ask 仅为防御性兜底，即便该类工具被请求也判 Ask）。运行时可切换模式（F47）。
- F46: 五层判定流水线——一次工具调用在**真正执行前**依次送过：① 黑名单 → ② 沙箱 → ③ 规则引擎 → ④ 模式兜底 → ⑤（兜底为 Ask 时）人在回路。判定只有三种中间值 **Allow / Deny / Ask**；其中**模式兜底层的值域严格为 {Allow, Ask}**，Deny 只可能来自黑名单、沙箱、显式 deny 规则、人在回路拒绝。任一层给出 Allow/Deny 即短路返回、不再进入后续层。其中黑名单仅对命令执行类、沙箱仅对文件类工具生效；**被跳过的层视为「未拦、继续下一层」**（如非命令执行工具跳过黑名单后继续走沙箱/规则，命令执行工具跳过沙箱后继续走规则/模式）。权限判定发生在 agent 编排层、与 Anthropic / OpenAI provider 无关。
- F47: 运行时模式切换——用户在会话中按 **Shift+Tab** 循环切换权限模式：default → acceptEdits → plan → bypassPermissions → default（四档全循环）。plan 另沿用既有 `/plan`（进）`/do`（出，固定切回 default 并触发按计划执行，不恢复进入 plan 前的模式）作为计划工作流的专用入口/出口，与 Shift+Tab 并存。**当前权限模式在状态栏常驻可见**（占据原 provider 名的位置；不再展示 provider 名）。切换只影响后续调用的兜底裁决，不改变已加载规则；**模式跨轮保持**（下一轮对话仍是上次切换的模式，不被重置）。
- F48: 人在回路交互——判定落到 Ask 时暂停 Agent Loop，向用户呈现待批准的工具调用（多行确认块：工具名 + 关键参数预览 + 触发原因 + 菜单选项），用户三选一，**↑↓ 移动光标 + 回车确认，数字键 1/2/3 亦可直选**，默认高亮「允许本次」：
  - **允许本次** → 判 Allow，不留记录；
  - **永久** → 判 Allow，并把对应 allow 规则写入**本地级**配置文件（跨会话生效）；
  - **拒绝本次** → 判 Deny（回灌让模型调整）。

  永久放行生成的规则采用**精确匹配**（针对该次具体调用的命令串或路径），不自动泛化。等待用户决策期间可取消（Esc / Ctrl+C）当前轮，取消后会话历史仍合法、可继续对话。
- F49: 拒绝回灌不中断——任何一层产生的 Deny（黑名单 / 沙箱 / deny 规则 / 人在回路拒绝），都包成**结构化错误结果**（含可读的被拒原因，按来源区分）回灌给模型，作为该工具调用的结果计入历史；Agent Loop 继续往下走，模型据此调整策略或换路径。同一批多个工具调用中，被拒的与放行的**各自独立**：结果按原调用序与各自调用 ID 正确配对回灌，互不串位、互不影响。

以下为 v0.7 新增（MCP 客户端接入）：

- F50: 两层配置加载与 MCP Server 声明——配置从**用户级**（`~/.config/wentian/config.yaml`）与**项目级**（`<项目根>/.wentian/config.yaml`）两个文件读取并**深合并**，项目级逐键覆盖用户级、新增条目并入；`providers` 与 `mcpServers` 两个块都适用此合并（项目级文件缺失时退化为仅用户级，与旧的单文件加载等价、向后兼容）。在配置中以一个映射 `mcpServers` 声明若干外部 Server，每条以 **Server 名为键**，按传输类型带不同字段：
  - **stdio 类型**：启动命令、参数列表、环境变量映射（在子进程环境基础上叠加）
  - **HTTP 类型**：服务地址（URL）、请求头映射
  - 环境变量值与请求头值支持 `${VAR}` 占位符，从当前进程环境展开；引用的变量缺失时展开为空串并记一条告警（不因缺变量而启动失败）
- F51: 协议会话三步走（JSON-RPC 2.0）——对每个 Server，客户端按 MCP 协议依次完成：① **初始化握手**（交换协议版本与能力声明，随后发送 initialized 通知）；② **列出工具**（取回每个工具的名称、面向模型的描述、输入参数 JSON Schema，以及可选的只读/危险注解）；③ **按名调用工具**（传入参数，取回内容结果或错误）。所有消息以 JSON-RPC 2.0 收发：请求带**唯一 id**，响应**按 id 与发起的请求关联配对**（即使响应乱序到达也能正确配对）；通知（如 initialized）无 id、不等待回应。
- F52: 两种传输——客户端支持两种传输，二者对上层暴露同一收发接口：
  - **stdio**：以子进程启动本地 Server，经其标准输入/输出收发按行分帧的 JSON-RPC 消息；子进程的标准错误作诊断输出、不参与协议。
  - **Streamable HTTP**：向配置的 URL 发 JSON-RPC 请求并带上配置的请求头，按 Streamable HTTP 规范处理响应（响应体可能是即时 JSON，也可能是 SSE 事件流，需从事件流中解析出 JSON-RPC 消息）。
- F53: 远端工具适配为统一工具——每个发现到的远端工具**包装成现有统一工具抽象**（名称、面向模型的描述、参数结构、执行入口），注册进工具中心，与六个内置工具并列；模型与 Agent Loop 调用它时与调用内置工具**完全一致**、对「这是远端工具」无感知。工具名做**命名空间隔离**（形如 `<Server名>__<工具名>`），保证不与内置工具及跨 Server 撞名。调用时把参数经协议发给对应 Server，取回的内容结果拼为文本回灌模型；远端报错或传输失败一律包装成**结构化工具错误**回灌（程序不崩溃、会话不中断）。
- F54: 外部工具安全默认——外部 MCP 工具**默认按「有副作用」对待**（执行前需确认、在权限体系里按最严兜底，即与内置副作用工具同档），**仅当** Server 在工具注解里明确标注只读（readOnlyHint=true）时才降为只读（可并发、免确认）。（与 v0.6 衔接：v0.6 权限系统本版只覆盖六个内置工具的细粒度规则；MCP 工具不进规则引擎的友好名路由，而是经此「默认有副作用」标志接入既有确认/模式兜底机制——类别无法判定按最严，符合 N16 安全默认。）
- F55: 多 Server 生命周期与故障隔离——启动时按配置逐个 Server 建立连接并发现工具；连接、握手、列工具任一步失败（命令不存在、超时、协议错、HTTP 不可达等）**只记一条告警并跳过该 Server**，不影响其余 Server 与程序启动；启动结束以可见方式汇报接入结果（成功的 Server 与其注册工具数、失败跳过的 Server 与原因）。已建立的连接在进程生命周期内**缓存复用**；程序退出时**统一关闭**（终止子进程 / 关闭 HTTP 会话），无僵尸进程残留。某 Server 运行中崩溃时其工具调用返回结构化错误、其余 Server 不受影响。本版**不做健康检查、不做自动重连**。无 `mcpServers` 配置时不输出多余信息、行为与 v0.6 完全一致。

以下为 v0.8 新增（上下文管理 + 双层压缩）：

- F56: 近似 token 估算（不上精确 tokenizer）——估算当前对话的 token 规模：以**上一次 API 返回的真实用量为锚点**，对锚点之后新追加的消息按**字符数近似折算**累加（字符/token 比可配，默认 3.5）；无锚点（首轮）退化为对全部消息做字符折算。两家后端的 `input_tokens` 语义不同——Anthropic 的 `input_tokens` **不含**缓存读/写（真实 prompt 总量需加 `cache_read + cache_creation`），OpenAI 兼容的 `prompt_tokens` **已含**缓存读（不可重复累加）；此差异由 **provider 层暴露统一的「真实 prompt token 总量」接口**消化，估算层对协议无感知。
- F57: 第一层·超大工具结果卸载（轻量预防，只碰工具结果）——每次请求前扫描历史中的工具结果消息，分两道闸：① **单条工具结果**估算 token 超过阈值（默认约 2K）→ 整条原始内容**存盘到会话产物目录**，对话里把该结果替换为**预览（前若干行/字）+ 文件绝对路径 + 一行提示**（「完整输出已存盘，需细节请用读文件工具读取」），并保留其错误标记；② **单轮内多条工具结果合计**超过阈值（默认约 8K）→ 按大小降序**挑最大的依次存盘**，直到该轮合计回落到阈值之下。卸载**幂等**（已卸载的不再重复处理），且**绝不改写用户消息与助手正文**（token 大头在工具结果，用户原始消息原文保留）。卸载产物随会话生命周期存放，本版不中途清理。
- F58: 第二层·结构化摘要兜底（重量，逼近窗口才触发）——当估算的对话总 token **逼近窗口上限**时，调用当前后端把**较早的消息**压成一段**结构化摘要**、**保留近期原文**：从历史**尾部按 token 往回保留**（约 1 万 token，且**至少 5 条消息**）作为原文窗口，更早的部分送 LLM 生成摘要。摘要按**八个固定部分**组织：① 主要意图 ② 关键概念 ③ 相关文件与代码 ④ 已修报错 ⑤ 问题求解 ⑥ 待办任务 ⑦ 当前工作 ⑧ 下一步。触发阈值 = **窗口上限 − 输出预留 − 安全余量**：窗口上限按 provider 配置 `context_window`、缺省取内置默认（如 200000）；输出预留 `reserved_output` 默认跟随 provider 的最大输出 token；安全余量自动触发为 13K、手动触发为 3K。切割点**必须落在干净边界**——绝不拆散「助手消息（含工具调用）↔ 其全部工具结果」，保留段**绝不以工具结果消息开头**（否则后端因工具调用/结果不配对而报错），被摘要掉那段携带的协议原始块（thinking / tool_use）随之丢弃。
- F59: 摘要 Prompt 纪律——生成摘要的请求**物理禁用工具**（请求不携带任何工具声明），且摘要 Prompt **明确禁止模型调用任何工具**；要求模型**先写分析草稿**（梳理对话脉络、定位关键信息）**再写正式摘要**，并声明**草稿用完即弃**、最终只采用正式摘要部分。
- F60: 压缩后边界消息——重量摘要完成后，在摘要之后**补一条边界消息**，提示模型：以上为早期对话的摘要，**需要文件的具体内容请重新用工具读取，不要照着摘要脑补或重建代码**。摘要与边界消息以**对后端角色合法**的形式承载（合并为一条 user 消息），与保留的近期原文段拼接成压缩后的新历史。
- F61: 手动触发 + 失败熔断——提供会话内命令 **`/compact`** 让用户**主动触发**一次重量压缩（安全余量收窄为 3K，用户主动要、可更激进）；重量摘要**连续失败 3 次**触发**熔断**：本会话后续**跳过重量兜底**（第一层预防照常工作）并告警一次，避免「摘要失败 → 历史不缩 → 立刻又触发又失败」的死循环；摘要**成功一次即清零**失败计数。手动 `/compact` **无视熔断标志强制重试一次**，成功则解除熔断。
- F62: 压缩触发编排——压缩在 **Agent Loop 每轮调用后端之前**触发（对应「每次 API 请求前」；因单个用户回合可能多轮、token 爆炸常发生在长循环中途，而非回合之间）：每轮先跑**第一层预防**（管单条/单轮工具结果大小），再判**第二层兜底**（管累积历史长度）。压缩**原地改写消息历史**，由 REPL 沿用既有「逐轮落盘」机制持久化（改写与卸载随当轮落盘，中途崩溃不留孤儿引用）。压缩为**可选装配**：未配置/未注入压缩器时 Agent Loop 行为与 v0.7 **字节级等价**；压缩注入点**独立于**既有 `request_decorator` 环境提醒通道（后者仍只读不写回、不持久化），两者互不干扰。

以下为 v0.9 新增（记忆与会话）：

- F63: 项目指令文件三层加载 + `@include`（填 `项目/自定义指令` 槽）——启动时从三处读取手写 Markdown 指令文件并**按优先级高在前拼接**注入系统提示的「项目/自定义指令」模块（该模块 v0.5 起恒返回空、本版起真渲染 `ctx.project_instructions`）：
  - **项目本地覆盖**：`<cwd>/.wentian/WENTIAN.md`（不提交，最高优先级、放最前）
  - **项目根**：`<cwd>/WENTIAN.md`（提交进仓、团队共享，次之）
  - **用户全局**：`~/.config/wentian/WENTIAN.md`（最低、放最后）
  - 每层文件可选，缺失静默跳过；三层均缺则该模块为空。
  - **`@include <相对路径>`**：指令文件中独占一行的 `@include xxx.md` 触发把目标文件内容**内联展开**（相对「包含它的文件所在目录」解析）。约束：**限嵌套深度**（默认 5，超限停止并告警）；**`visited` 集合防环**（同一文件在一条 include 链上重复出现即跳过并告警，不无限递归）；**拦截逃出项目根的路径**（先解析符号链接再前缀比对，与 v0.6 沙箱同规——解析后若落在项目根之外或为越界绝对路径 → 拒绝该 include 并告警，不读取）。
  - 拼接后总体积设上限（防撑爆上下文），超限按上限截断并告警。
- F64: 会话存档改 JSONL 追加 + 按 cwd 分区存储——会话从「单文件全量 JSON 重写」改为**每会话一个 JSONL、按消息追加写**：
  - 路径 `~/.local/share/wentian/projects/<cwd-slug>/sessions/<id>.jsonl`（`<cwd-slug>` = 绝对 cwd 路径把分隔符 `/`→`-`，与 Claude Code 同法、可读可调试；id 仍为 `YYYYMMDD-HHMMSS-xxxx`）。
  - **追加写**：每轮把新增消息**逐行追加**（不重写全文）；追加快、崩溃只丢最后半行。首行可选 `meta` 记录（id/created_at/provider），其后每行一条 Message。
  - **不维护独立 meta 文件**：ID 取文件名、标题取首条 user 消息行、消息数=数消息行、updated_at 取文件 mtime（或末行）。
  - **分区与列举**：`/sessions` 与 `--continue` **默认只扫当前 `<cwd-slug>` 分区**；`/sessions --all` 跨 `projects/*/sessions/` 全扫。旧全局扁平 `~/.local/share/wentian/sessions/*.json` 视为**遗留**：不列、不删、不报错。
- F65: 会话恢复卫生——`load`/`resume` 时对历史做四道卫生处理，保证恢复后历史**对两家 provider 合法**且**不撑爆窗口**：
  - ① **坏行跳过**：JSON 解析失败的行跳过并告警 stderr，不抛给调用方（JSONL 原生能力）。
  - ② **尾部未配对 tool_call 截断**：历史尾部若是「助手 tool_call 无后续工具结果」的悬空调用 → 截断该未配对部分（送两家 provider 转换不产生 400）。
  - ③ **溢出先压一次**：恢复后用 v0.8 估算判定 `estimate_total > context_window - margin` → **调 v0.8 `Compactor.compact()` 压一次**再进入对话（复用，不重造）。
  - ④ **时间跨度提醒**：距上次 `updated_at` 超阈值（`sessions.resume_gap_reminder_hours`，默认 4h）→ 恢复后**首轮**经 v0.5 `<system-reminder>` 消息通道注入一条**一次性**时间跨度提示（绝不写回持久化 messages）。
- F66: 过期会话清理——启动时**惰性清理**：删除当前分区内 `updated_at`（mtime）早于 `sessions.retention_days`（默认 30 天）的会话文件及其 `.artifacts/` 目录；清理失败不致命（告警跳过）。
- F67: 自动记忆抽取（后台线程·每轮）——每个 `AgentDone.stop_reason == COMPLETED` 回合后启 **daemon 线程 fire-and-forget**：抽取器取**最近一轮对话**（末次 user + 助手正文，含必要工具活动摘要）+ 现有 INDEX，调 LLM（默认复用当前对话 provider，`memory.provider` 可覆盖；为线程安全**自建 provider 实例**、不跨线程共享对话 provider）产出**四类笔记**——**用户偏好 / 纠正反馈 / 项目知识 / 参考资料**；**去重交 LLM 判断**（把现有索引喂给它，判「新增 / 更新已有 / 跳过已覆盖」）。抽取**绝不阻塞 REPL 输入**、异常静默吞到 stderr、**绝不中断会话或污染对话历史 messages**；并发写 INDEX **加锁串行**；`/exit` 时最多 `join` 一个短超时不卡退出。`memory.enabled: false` 时整条链路关闭。
- F68: 记忆分级存储 + 索引启动注入（填 `长期记忆` 槽）——笔记**用户级与项目级分开存**：
  - **用户级** `~/.config/wentian/memory/`：默认收 `用户偏好`、`纠正反馈`（跨项目）。
  - **项目级** `<cwd>/.wentian/memory/`：默认收 `项目知识`、`参考资料`（LLM 可改判归属）。
  - 每条笔记 = 一个带 **frontmatter**（category / created_at / source_session / tags）的 Markdown 文件；每域一份 **`INDEX.md`** 摘要索引（条目标题 + 一句话，非全文）。
  - **启动注入**：build_app 时读两份 `INDEX.md` 拼进 `ctx.memory` → 填「长期记忆」模块（该模块 v0.5 起恒空、本版起真渲染）；索引**≤ 200 行 / 25KB**（用 v0.8 estimator 卡预算），超限按上限截断。**启动注入一次、不热刷**（保 v0.5 提示词缓存稳定）；本会话新抽的记忆**下一会话**生效——正合「新会话启动时恢复」。
- F69: 配置扩展（沿用 v0.7 两层深合并，全可选带默认、缺块/缺字段安全降级不抛异常）：
  - 顶层 `memory:` 块——`enabled`(默认 true) / `provider`(可选，缺省复用当前对话 provider) / `max_index_lines`(默认 200) / `max_index_bytes`(默认 25600)。
  - 顶层 `sessions:` 块——`retention_days`(默认 30) / `resume_gap_reminder_hours`(默认 4)。

以下为 v0.10 新增（斜杠命令系统）：

- F70: 命令注册中心 + 命令定义元数据——一个**命令注册中心**按注册顺序管理每条命令的元数据，每条命令登记：**名称**（规范名、无斜杠、小写）、**别名**（零个或多个）、**简短描述**（`/help` 一行说明）、**用法示例**（如 `/session resume <id>`）、**命令类型**（本地 / 影响界面 / 提示词三选一）、可选**参数提示**、可选**是否隐藏**、**处理函数**。注册中心提供按名/别名查找、按注册顺序列举可见命令、按前缀给补全候选。**启动期冲突即硬失败**：注册时若命名或别名与已注册项冲突 → 立即 `raise`（在 `build_app` 启动阶段触发、进程带 traceback 退出），不延后到运行时（镜像既有 `ToolRegistry` 重名 `raise` 模式）。
- F71: 命令解析器——识别 `/` 前缀：第一个空格前为**命令名**、之后为**参数串**；命令名**转小写**做到大小写不敏感（`/Help` ≡ `/help`）；**空输入早返回**（裸 `/` 或纯空白不进分发）；解析出命令名后在注册中心**未命中** → 打印「未知命令 + `/help` 引导」、不送 AI。
- F72: 三类执行模式——命令按执行模式分三类：**纯本地**（跑完即返回、不扩展对话历史，如 `/help`/`/status`）、**影响界面状态**（改 REPL/会话状态，如 `/plan`/`/clear`/`/session`/`/provider`）、**提示词**（把预设提示词送进对话交给 AI 跑一轮，如 `/review`）。类型既是 `/help` 分类展示的元数据，也约束分发行为（提示词类经界面接口的「发送用户消息」触发一轮 AI）。
- F73: 界面控制接口（命令与渲染框架解耦）——抽一层**界面控制接口**供命令处理函数调用，至少覆盖：**显示消息** / **发送用户消息**（提示词类用）/ **切换模式** + 查当前模式 / **查 token 用量** / **查/刷状态** / 清空当前上下文 / 会话操作（新建 / 列举 / 恢复）/ 切换 provider / 触发压缩 / 查记忆摘要 / 列举可见命令。命令实现只依赖该接口、**不绑定具体渲染框架**（不直接碰 Rich）；REPL 实现该接口。**状态栏联动模式标记**：状态行左段模式标记改为括号式 `[DEFAULT]`/`[PLAN]`（及 acceptEdits / bypassPermissions 对应标记），随 Shift+Tab / `/plan` / `/do` / `/permission` 切换实时反映。
- F74: 输入入口分流器——在用户回车的入口（REPL 主循环既有 `line.startswith("/")` 判断）保留分流器：**是命令** → 走本地命令分发（解析 + 查注册中心 + 调处理函数），**不是命令** → 原样送给 AI 跑一轮（v0.4 起的 AgentLoop 路径不变）。
- F75: 别名 + Tab 补全——支持命令**别名**（多别名指向同一命令）；输入以 `/` 开头**且尚在敲命令名**（无空格）时按 Tab 触发补全：**单匹配直接补全**、**多匹配弹菜单**、**隐藏命令不参与**补全与 `/help`。补全候选带一行描述。Shift+Tab 仍归切权限模式（已占用、不与 Tab 冲突）。
- F76: 内置十个高频命令 + 归并老命令——内置 `/help`（含别名）、`/clear`（清空当前会话上下文、留同一会话 id）、`/compact`、`/plan`、`/do`、`/session`（`new` / `list [--all]` / `resume <id>`，折叠原 `/new`/`/sessions`/`/resume`）、`/memory`（展示长期记忆目录 + 各域 INDEX 摘要，只读）、`/permission`（展示当前模式；带参切档，等价 Shift+Tab）、`/status`（展示模式 / 会话 / 消息数 / 上轮 token）、`/review`（提示词类，把「审查未提交改动」预设提示送进对话）；并把 v0.1 起临时手写的 `/provider`、`/exit` 作为独立命令一并登记。**全部命令走同一条注册中心分发路径**，无第二套旁路。

以下为 v0.11 新增（Skill 系统）：

- F84: Skill 定义格式（YAML frontmatter + Markdown 正文）——一个 Skill = 一份带 **YAML frontmatter** 头 + **Markdown 正文** 的文档。frontmatter 放元信息、正文是发给模型的 SOP 指令。frontmatter 字段：**`name`**（唯一标识、无斜杠、小写，斜杠命令名来源；缺失 → 该文件视为非法跳过）、**`description`**（一句话说明，进启动菜单与 `/help`）、可选 **`allowed_tools`**（可见工具白名单，工具名列表；省略 = 不收窄）、可选 **`mode`**（`shared`（默认）/ `isolated`）、可选 **`history`**（仅 `isolated` 用，带多少条主对话历史进子对话，整数，默认 `0`）、可选 **`model`**（指定模型覆盖，缺省复用当前对话模型）。正文支持**参数占位符**：`$ARGUMENTS`（整条参数串）与 `$1`/`$2`…（按空格切的位置参数）；激活/执行时把用户/模型传入的参数替换进正文（无对应参数的占位符替换为空串）。两种物理形态等价：**单文件** `<name>.md`，或**目录型** `<name>/SKILL.md`（目录可另含 `tools/` 子目录存放专属工具的 schema 与实现脚本——本版**仅识别目录结构、不加载执行**该脚本，作为后续章节的分发占位）。
- F85: 三层发现 + 同名覆盖 + 解析容错——Skill 从三层目录发现，优先级**高→低**：**项目级** `<cwd>/.wentian/skills/` ▸ **用户级** `~/.config/wentian/skills/` ▸ **内置**（随包分发，`importlib.resources` 读取）。同 `name` 高层**整体覆盖**低层（不做字段级合并）。单个 Skill 文件**解析失败**（YAML 坏 / 缺 `name` / 读不出）→ **静默跳过该文件、不阻断**其余 Skill 的发现（镜像 v0.9 指令缺文件跳过）。发现结果汇成一个 **Skill 注册表**（与 v0.10 命令注册中心并列的数据结构，纯数据、零编排依赖）。
- F86: 两阶段加载（启动菜单 + 系统级加载工具）——**阶段一（启动·菜单）**：启动时把全部已发现 Skill 的 `name + description` 注入系统提示一个**稳定的「可用 Skill」模块**（复用 v0.5 起预留、v0.9 仍恒空的「已激活 Skill」槽位改渲菜单）；该模块只随 `/skills reload` / `/clear` / 会话切换变化，**对话内逐轮稳定**、不破坏 v0.5 提示词缓存前缀。**阶段二（按需·正文）**：提供一个**系统级**工具 `load_skill(name, args?)`，**恒可见、不受任何 Skill 白名单约束**（即便白名单收窄到很小，加载工具始终在）；模型按菜单里的 `name` 调用它来激活某个 Skill。
- F87: 激活态每轮注入 + 白名单收窄（共享模式）——激活集为 REPL 会话内状态、**可多个 Skill 同时激活**。`shared` 模式 Skill 被 `load_skill` 激活后：① **每轮**模型请求前，经 v0.5 `<system-reminder>` 消息通道（v0.8 起的 `request_decorator`）在**最新用户消息处**追加每个激活 Skill 的「`# 已激活 Skill: <name>` + 完整正文（占位符已替换）」——**每轮重建、绝不写回持久化 messages**（沿用 decorator 不 mutate 入参契约）；② **工具白名单收窄**复用 v0.4/v0.6 的 AgentLoop `allowed_tools` 机制（声明过滤 + 运行时拦截双层，与计划模式同款）：激活集非空且**每个**激活 Skill 都声明了 `allowed_tools` → 当前可用工具 = 各白名单**并集** ∪ `{load_skill}`；激活集中**任一** Skill 省略 `allowed_tools` → **不收窄**（全工具）；激活集为空 → 全工具。`load_skill` 永远在可用集内。
- F88: 启动白名单校验（fail-fast，区别于解析跳过）——启动发现 Skill 后，对每个**成功解析**的 Skill 校验其 `allowed_tools` 的**每一项都存在**于当前工具注册表（含 MCP 适配后的远端工具）；校验**必须排在 MCP 发现注册之后**进行（否则远端工具会被误判不存在）。一旦出现白名单引用了**不存在的工具名** → 在 `build_app` 启动阶段**立即 `raise` 中止启动**（panic、带 traceback、指明哪个 Skill 的哪个工具名非法）。这是有意的**非对称**：文件**解析失败**是静默跳过（坏文件不该拖垮整体），而白名单引用错工具是**配置硬错误**、必须当场炸出来让用户改（镜像 v0.10 命令冲突 panic、v0.3 工具重名 raise）。
- F89: 独立模式执行 + 摘要回流——`isolated` 模式 Skill 被 `load_skill` 命中时，**当场开一个嵌套子对话**跑完再回流：取主会话**末 `history` 条**消息作子对话起始上下文（默认 0 = 不带历史）；子系统提示 = 主系统提示 + 该 Skill 正文段；子可用工具 = 该 Skill 白名单（规则同 F87，省略则全工具）；子模型 = `skill.model` 或当前模型。子对话跑一个**独立的 AgentLoop**（在**独立 worker 线程内跑自己的事件循环**，避免与主循环嵌套 `asyncio.run` 冲突），跑完取**子对话最终助手正文**作为 `load_skill` 的**工具结果**返回主循环——于是摘要天然落进主对话历史的一条 tool result。不做额外的 LLM 二次摘要（正文可自行指示子 agent「末尾给一段简洁总结」）。独立模式 Skill **不进激活集**、不污染后续轮、不收窄主循环工具。
- F90: 斜杠自动注册 + `/skills` + 热更新 + 清空联动 + 内置三样板——每个已发现 Skill **自动注册成一条提示词类（PROMPT）命令** `/<name> [args]` 进 v0.10 命令注册中心（于是出现在 `/help`、参与 Tab 补全、走同一条分发路径）；用户敲 `/<name> 参数` 等价于模型调用 `load_skill(name, "参数")`。**命名冲突策略**（避免 v0.10 注册中心启动 panic）：Skill 之间的同名冲突已由 F85 三层覆盖在注册前消解；Skill 名与**提示词类内置命令**（如 `/review`）冲突 → **Skill 替换该命令**（富化优先，本版即让内置 `review` Skill 接管 v0.10 的 `/review` 预设）；Skill 名与**控制类内置命令**（`/exit`/`/clear`/`/help`/`/plan`/`/do`/`/session`/`/compact`/`/permission`/`/status`/`/provider`/`/memory`/`/skills`，受保护）冲突 → **跳过其斜杠注册并告警**，但该 Skill 仍可经 `load_skill` 工具加载（不丢能力、不 panic）。另提供：**`/skills`**（列全部已发现 Skill：名 + 描述 + 来源层 + mode，只读）；**`/skills reload`**（重扫三层、重建 Skill 注册表与斜杠命令、重跑 F88 白名单校验——reload 期校验失败**保留旧注册表 + 打印错误、不崩会话**，区别于启动期 fail-fast）。**清空联动**：`/clear` 与 `/session new` 在清空对话时**顺带清空激活集**。**内置三样板 Skill**（随包分发，最低优先级、可被用户层覆盖）：`commit`（暂存改动 + 写 conventional commit，对齐项目 CLAUDE.md：中文/英文随项目、**绝不加 `Co-Authored-By`**、不主动 push）、`review`（git diff 评审，可配 `isolated` 带少量历史）、`test`（跑测试并报告）。

以下为 v0.12 新增（Hook 生命周期系统）：

- F77: Hook 规则模型（事件 + 条件 + 动作三要素）+ YAML 声明式加载与集中校验——一条 Hook 规则由三要素描述：**`event`**（触发时刻，必填）、**`if`**（条件表达式，可省略 = 无条件触发）、**`action`**（动作，必填，含类型与各自字段）。规则从配置文件顶层 **`hooks:`** 块声明式加载（一个规则列表），由 `hooks` 包**集中校验**：事件名非法、动作缺必填字段、拦截类事件标了后台异步、超时非正数等一律在**加载期**报 `HookConfigError`（启动失败、带定位信息），不延后到运行时才炸。`hooks:` 整块缺失 → 零规则（行为同上一版）；用户级 + 项目级两层配置的 `hooks` 列表**叠加生效**（两层规则都装载、不互相覆盖——与标量/字典块的「就近覆盖」不同，规则天然累积）。
- F78: 生命周期事件覆盖四层 + 系统级（十事件）——在 Agent 生命周期关键缝挂事件：**会话级** `SessionStart`（会话起）/`SessionEnd`（会话止、含退出原因）；**消息级** `UserPromptSubmit`（用户提交一条消息）/`Stop`（一次用户轮 Agent 收束、含停机原因）；**轮次级** `RoundStart`/`RoundEnd`（ReAct 推理轮起止）；**工具级** `PreToolUse`（工具执行前）/`PostToolUse`（工具执行后、含结果）；**系统级** `PreCompact`（上下文压缩前、含触发来源 manual/auto）/`Notification`（Agent 需用户关注时、如权限询问）。每个事件携带一份**事件上下文**（cwd / session_id / 该事件特有字段如工具名·参数·提示词·停机原因等）供条件匹配与动作消费。事件触发挂在既有缝上、**不改 AgentLoop 事件契约**（loop 不感知 hooks，由装配层在消费事件处触发）。
- F79: 工具执行前拦截（PreToolUse 细粒度安全策略）——`PreToolUse` 事件能**拦截**工具执行：基于工具名与**工具参数**（命令串 / 文件路径等）做细粒度安全策略；拦截信号 = 规则的 **shell 动作以退出码 2 结束**（命令产出的 stderr/stdout 作拒绝原因），被拦下后把**拒绝原因当工具结果回灌给模型**（`is_error=True`），让模型据此调整、主流程不中断。拦截在既有**权限五层判定之前**跑：hook 命中拦截 → 合成拒绝结果短路、不进权限门、不进 executor；hook 不拦 → 落既有权限管线。**hook 只能加约束、不能放权**（不能 allow 越权跳过权限确认）；shell 之外的动作（prompt/http/subagent）在 `PreToolUse` 上只作副作用 / 注入、不具拦截力。
- F80: 条件表达式（四模式匹配 + 全部 / 任一逻辑组合）——条件 `if` 由 **`match`**（逻辑组合：`all` = 全部满足 / `any` = 任一满足，二选一不混用、缺省 `all`）与 **`clauses`**（子句列表）构成；每个子句 `{field, pattern}` 取事件上下文的具名字段与模式比对。模式**复用权限规则的匹配语义并扩展为四种**：**精确**（字面相等）、**反向**（`!` 前缀取反）、**正则**（`/.../` 包裹）、**glob**（`*`/`?` 通配）。匹配原语抽成**共享叶子** `textmatch.match_one(pattern, value)`（纯 stdlib），hook 条件用它；缺失字段按空串参与匹配。`if` 省略 ⇒ 无条件恒触发。
- F81: 四种动作类型——动作 `action` 按 `type` 分四类：**`shell`**（执行 shell 命令——事件上下文以 JSON 经 stdin 传入、关键字段另以 `WENTIAN_HOOK_*` 环境变量暴露；`PreToolUse` 下退出码 2 = 拦截）；**`prompt`**（注入提示词——文案支持 `{field}` 占位符按上下文替换，产出文本进**待注入缓冲**）；**`http`**（发 HTTP 请求——用 stdlib `urllib` 发，事件上下文作 JSON 请求体，零新增依赖）；**`subagent`**（启动子 Agent——**本版占位**：记一条「未实现」日志，真实运行留 SubAgent 章节对接）。任一动作失败软化（见 F83）。
- F82: 执行控制（只跑一次 / 后台异步 / 命令超时）——三种执行控制：**`once`**（标记后本会话只触发一次、再次命中即跳过；**持久化本版不做**——「已跑过」状态仅存内存、跨会话不保留）；**`background`**（动作在 daemon 线程 fire-and-forget 异步跑、不阻塞主流程）；**`timeout`**（shell/http 动作的执行超时秒数，超时即终止并记日志）。**拦截类事件（`PreToolUse`）不允许异步**——`background: true` 配在 `PreToolUse` 规则上即在**加载期**校验失败（拦截必须同步出结果）。
- F83: 失败软化 + 主流程零中断 + Hook 引擎装配——**Hook 自身失败只记日志、绝不中断 Agent 主流程**：任一动作抛异常 / 超时 / 脚本缺失都被捕获写进 hook 日志、不冒泡；`PreToolUse` hook **失败即 fail-open**（落既有权限门兜底，破损的安全 hook 不能把 Agent 卡死——五层权限系统仍是硬后盾）。Hook 引擎 `HookEngine` 按 `hooks` 配置构造、**鸭子注入**装配层（仿 `permission_gate`/`compactor`/`memory_runner`/`commands` 的可选注入：无 hooks ⇒ 不注入、回退上一版行为）；引擎对 agent/loop/repl/provider/tools **零反向依赖**、由装配层在各事件缝调用。注入提示词经引擎累积、下次请求随既有 `<system-reminder>` 通道（`request_decorator`）下发，不写回 messages、不持久化。

以下为 v0.13 新增（子 Agent 委派）：

- F91: 统一 Agent 工具 + type 分流 + 工具列表稳定——只向工具注册中心登记**一个** `Agent` 工具（`category=COMMAND_EXEC`、需确认，与既有副作用工具同档），它对模型暴露一份稳定输入 schema：**`type`**（`"definition"` / `"fork"`，必填，决定走哪条委派路径）、**`agent_type`**（角色名，`definition` 必填、`fork` 忽略）、**`prompt`**（要交给子 Agent 的任务，必填）、可选 **`background`**（bool；`fork` 恒视为 `true`）。`type` 仅在工具内部分流两条执行路径（定义式 F94 / Fork 式 F95），**对模型始终只暴露这一个工具**——无论加载了多少角色，工具列表里 Agent 工具的数量与 schema 都不随角色增减而变（角色清单经系统提示/`description` 让模型可见，而非膨胀工具数）。复用 v0.3/v0.4 `Tool` 基类与工具注册中心，新增的 Agent 工具与六个内置工具及 MCP 工具并列、对 Agent Loop 多轮调用无感。
- F92: 角色定义格式（Markdown + frontmatter）——一个角色 = **frontmatter 头 + Markdown 正文**，正文即子 Agent 的**系统提示**（定义其身份 / 职责 / 工作风格，随整个子对话生命周期注入、不是一次性消息）。frontmatter 字段：**`name`**（唯一标识、小写无斜杠，是角色清单与 `agent_type` 的索引键；缺失 → 该文件视为非法、跳过）、**`description`**（用途说明，进 `/agents` 列表与**模型可见的角色清单**，供模型判断该派哪个角色）、可选 **`tools`**（工具白名单，列表）、可选 **`disallowed-tools`**（工具黑名单，列表）、可选 **`model`**（`inherit`（默认）/ `haiku` / `sonnet` / `opus`，经 F100 映射）、可选 **`max-turns`**（子 Agent 单次执行的最大轮次，缺省取 `AgentsConfig` 默认）、可选 **`permission-mode`**（子 Agent 初始权限 `Mode`）。格式镜像 v0.11 Skill 的「frontmatter + 正文」结构，但语义不同：Skill 正文是一次性 SOP，角色正文是**伴随子 Agent 全程的系统提示**。
- F93: 角色四源加载 + 同名覆盖 + 解析容错 + 共享原语——从四层目录发现角色，优先级**高→低**：**项目级** `<cwd>/.wentian/agents/` ▸ **用户级** `~/.config/wentian/agents/` ▸ **内置**（随包分发，`importlib.resources` 读取）▸ **插件**（最低层；本版无插件系统 ⇒ 惰性空源，仅预留优先级位、不实际加载）。同 `name` 高层**整体覆盖**低层（不做字段级合并）。单文件**解析失败**（frontmatter 坏 / 缺 `name` / 读不出）→ **静默跳过该文件、不阻断**其余角色发现（镜像 v0.9 指令缺文件跳过、v0.11 Skill 解析容错）。发现结果汇成一个 **AgentRegistry**（name→AgentDef，纯数据、零编排依赖，与 v0.10 命令注册中心 / v0.11 Skill 注册表并列）。frontmatter 的解析**复用从 v0.11 `skills/loader.py` 抽出的共享原语**（`_split_frontmatter` / `_parse_frontmatter` 抽到共享位置，skills 与 agents 同调一份解析函数；agents 提供自己的 discovery 与 `AgentDef` dataclass）——抽取后 v0.11 Skill 解析行为保持不变。
- F94: 定义式子 Agent 执行（前台跑到底、只回结果）——`type=definition` 时按 `agent_type` 从 AgentRegistry 取角色，从**空白对话**起一个隔离的子 Agent：子系统提示 = 角色正文（+ 必要环境块）；子可用工具 = 经 F97 三层过滤后的集合；子模型 = 角色 `model` 经 F100 映射；子 `max-turns` / `permission-mode` 取角色 frontmatter。**默认前台阻塞**——装配一个全新的 `AgentLoop`（复用 v0.4 循环核心，传入隔离的 `allowed_tools` / `permission_gate` / `max_rounds`），在**独立 worker 线程内跑自己的事件循环**（避免与主 `asyncio.run` 嵌套冲突，机制同 v0.11 isolated Skill 子对话），一路跑到 `COMPLETED` 停机（回复中不再请求工具），抽取**末条助手正文**作为 Agent 工具的结果同步返回。**主会话只落「调用 Agent 工具 → 得结果」一条 tool result**，子 Agent 内部的多轮工具往返**不写进主会话持久化记录**（上下文隔离根治——主对话只见调用与结果，不见子对话的中间过程）。前台运行期间 REPL 显示**紧凑状态**（角色名 / 计时 / 已发起工具调用数），不把子对话全量刷屏。
- F95: Fork 式子 Agent（继承历史 + 命中缓存 + 强制后台）——`type=fork` 时子 Agent **继承父对话的完整消息历史 + 父工具集**作为起始上下文（无需 `agent_type`，不依赖角色）：因子请求前缀与父对话一致 → **首次请求即命中 v0.5 prompt cache**，借已缓存前缀省钱省时延。Fork **强制走后台**（`background` 恒 `true`），不阻塞主 Agent。Fork 子 Agent 仍受 F97 工具过滤约束（至少**全局禁止 Agent 工具**以防嵌套自繁殖）；跑完后其结果经 F99 异步回灌主对话。Fork 与定义式的区别在「起始上下文来源」与「是否强制后台」——前者继承父历史走后台，后者空白对话默认前台。
- F96: 运行时状态隔离 + 基建共享——每个子 Agent 持有**完全独立**的运行时状态：自己的消息列表、权限模式 `Mode`、权限追踪（allow-always 集）、文件读状态、token 计数；这些状态**绝不回流主会话、也不串到其他并发子 Agent**。同时**共享无状态基建**：provider 工厂 / `ProviderConfig` / 凭据（但每个子 Agent 经 `create_provider` 造**全新 provider 实例**以保并发安全）、`HookEngine`、文件系统工作目录。隔离边界明确——子 Agent 改了自己的权限模式或读了某文件，主 REPL 的 `_mode`、主会话的读状态、别的子 Agent 的状态都不受影响（复用 v0.6 无状态 `PermissionPipeline.decide` 让每个子 Agent 独立建门成为可能）。
- F97: 工具过滤三层防线 + 嵌套防护——子 Agent 的最终允许工具集 = `(全部工具 − 全局禁止) ∩ 角色白名单 − 角色黑名单`，若该子 Agent 走后台再 `∩ 后台白名单`，全程**纯函数集合运算**。**① 全局禁止**：`Agent` 工具本身对**任何**子 Agent 永不可见 ⇒ 子 Agent 无法再生子 Agent（嵌套防护的第一道线）；另加**深度兜底**——子 Agent 上下文带深度标记，深度 ≥ 1 即拒绝再起子 Agent（即便角色白名单误列了 Agent 也被全局禁止 + 深度标记双重挡下）。**② 角色额外限制**：角色 frontmatter 的 `tools` 白名单 / `disallowed-tools` 黑名单进一步收窄（白名单交集、黑名单差集，复用 v0.4/v0.6 `allowed_tools` 语义）。**③ 后台白名单**：后台子 Agent 无人在回路确认 ⇒ 只放**免确认工具**（`READ_ONLY` 类 + `AgentsConfig.background_allow` 显式放行项），需确认的 `FILE_WRITE` / `COMMAND_EXEC` 类一律过滤掉，从根上不让后台任务卡在确认阻塞上。
- F98: 后台任务管理器（三种进后台 + Fork 恒后台）——一个内存态 `BackgroundTaskManager` 追踪每个后台任务的 `{id, 角色/类型, prompt, status(running/done/failed), result, usage(token 用量)}`。任务进后台有**三条路径**：① **显式**——Agent 工具 `background=true` 或角色声明走后台；② **超时自动**——前台子 Agent 运行超过 `AgentsConfig.foreground_timeout_s` 阈值 → 转入后台，Agent 工具立即返回「已转后台，id=X」（可注入时钟以离线测超时）；③ **手动切**——前台子 Agent 运行期间用户按键 → 推入后台（复用 / 扩展 v0.4 的 interrupt 中断通道）。**Fork 式恒走后台**。后台任务在 **daemon 线程**内执行，进程退出时统一短 `join`、不泄漏线程（线程纪律同 v0.9 后台记忆抽取）。
- F99: 异步结果回灌——按前台 / 后台分两条回流路径。**前台**子 Agent → Agent 工具**阻塞到 `COMPLETED`、直接同步返回**末条助手正文（F94）。**后台 / Fork** → Agent 工具**立即返回**「任务 id=X 已起」占位结果；子 Agent 跑完后把最终结果写入管理器的**完成回灌缓冲**，由主 Agent 的 `request_decorator` 在**下一轮请求前** `drain` 出来，包成 `<system-reminder>` 注入消息流（**复用 v0.5/v0.8 注入通道，不写回 messages、不持久化**，契约同既有环境提醒）。于是主 Agent 在下一轮即「得知」后台任务完成及其结果摘要，而中间往返不污染主对话历史。
- F100: 模型层级映射 + AgentsConfig——角色 `model` 别名（`inherit` / `haiku` / `sonnet` / `opus`）→ 具体模型 ID 的映射放在 **`AgentsConfig.model_aliases`**：缺省内置 Anthropic 当代模型 ID（`haiku` → `claude-haiku-4-5`、`sonnet` → `claude-sonnet-4-6`、`opus` → `claude-opus-4-8`），用户可在配置里覆盖 / 增补；**`inherit`**（默认）⇒ 取主对话 provider 当前所用模型。子 Agent 的 provider 经 `dataclasses.replace(主 ProviderConfig, model=映射后 ID)` + `create_provider` 造出全新实例（复用 v0.x provider 工厂，不重造凭据 / 协议）。非 Anthropic provider 用 `inherit` 或用户自配别名即可。**别名不可解析**（既非内置也未在配置声明）→ Agent 工具直接返回清晰报错、**不空起一个子 Agent**（安全默认：宁可报错也不带着错模型跑）。
- F101: `/agents` 命令（列出 + 查看）——复用 v0.10 命令注册中心登记一条 **`/agents`**（LOCAL 类，经鸭子注入的管理器句柄读数据、不发 provider 请求）：**无参** → 列出全部后台任务（id / 角色或类型 / 状态 / 起始时刻的相对值 / token 用量）；**`/agents <id>`** → 查看该任务的**结果全文 + 用量**。命令走 v0.10 同一条注册中心分发路径、进 `/help` 与 Tab 补全。**取消 / kill 本版不做**（`/agents` 只读，留后续章节）。
- F102: SubAgentAction 接通——把 v0.12 `hooks/spec.py` 里的 **`SubAgentAction`**（v0.12 仅占位「本版不执行实际子 Agent」、记一条未实现日志）**真正接通**：当 hook 规则的 `subagent` 动作触发时，经后台管理器 / runner **真起一个定义式子 Agent**（按角色名或内联 prompt），fire-and-forget 落入后台，结果经 F99 回灌主对话。动作失败仍遵 v0.12 **失败软化铁律**（只记 hook 日志、绝不冒泡中断主流程）——把 v0.12 留的占位坑填上、且不破坏 v0.12 的软化契约。

## 非功能需求

- N1: 实现语言为 Python（用户指定）
- N2: 优先复用各后端官方 SDK，流式、鉴权、重试等细节交给 SDK，不手写 HTTP/SSE
- N3: 默认模型为当前最强 Claude 模型，不擅自降级
- N4: 配置文件与会话数据存放遵循 XDG 标准用户目录
- N5: 单一职责分层（配置 / 后端 / 会话 / 界面相互解耦），核心逻辑可在不联网的情况下被自动化测试
- N6: （v0.3）工具的定义与执行不依赖任何后端 SDK；协议差异（工具声明格式、流式解析、结果回传格式）全部在后端层内消化，工具层对协议无感知
- N7: （v0.3）全部六个工具的执行逻辑可在不联网的情况下被自动化测试（在临时目录中真实读写文件、执行无害命令）
- N8: （v0.4）循环核心对协议与具体工具无感知（仅依赖统一后端接口与执行器接口）；循环全链路（事件流、双路收集、安全分批、五停机条件）可在不联网的情况下被自动化测试
- N9: （v0.5）系统提示的模块组装与消息提醒的拼装都是纯函数，可在不联网下单独测试每个模块、整体拼装顺序、提醒注入与不持久化；缓存断点的施加与缓存用量字段的解析全部在后端层内消化，prompt 组装层与提醒拼装层对协议无感知（不依赖任何后端 SDK）
- N10: （v0.6）黑名单不可绕过——不存在任何配置项、规则或模式（含 bypassPermissions）能放开黑名单命中的命令；黑名单是流水线最高优先级，先于其余四层
- N11: （v0.6）沙箱防逃逸——路径判断必须「先解析符号链接、再前缀比对」，软链接指向项目外的目标判 Deny；对新建文件按最近已存在祖先目录解析，不因目标不存在而误判通过
- N12: （v0.6）不破坏 v0.4/v0.5——Agent Loop 多轮编排、流式双路收集、保序分批并发、用户取消、历史一致、缓存与 system-reminder 注入等既有能力不退化；只读工具的并发执行不因权限检查退化为串行（只读永不触发 Ask）
- N13: （v0.6）人在回路可取消、不死锁——等待用户决策期间按 Esc / Ctrl+C 能干净地取消本轮（不泄漏挂起的 asyncio task、不死锁、不退出整个程序）；取消后对话历史角色交替合法、工具调用与结果配对不破坏
- N14: （v0.6）配置降级——三层配置文件缺失视为空；格式非法时跳过该文件并降级为安全默认（不额外放开权限），不抛未捕获异常、不中断会话、不致引擎构造失败
- N15: （v0.6）跨协议一致——权限判定与 provider 无关，Anthropic 与 OpenAI（含兼容端点）行为完全一致；不改 provider 适配层
- N16: （v0.6）安全默认——未知/未注册工具、参数无法解析、类别无法判定等不确定情形，一律按最严处理（按有副作用工具走 Ask、或直接 Deny），绝不静默放行
- N17: （v0.6）代码规范——`ruff format --check .` 与 `ruff check .` 全部通过（可选 `mypy`），遵循 CLAUDE.md
- N18: （v0.6）可扩展性——新增一层防御、一档模式或一类规则匹配时改动局部：不触及判定流水线主编排、不改 provider 适配层；权限引擎为纯包（零后端 SDK / 零 rich / 零 prompt_toolkit import），可在不联网下单测每一层
- N19: （v0.7）MCP 协议层 stdlib 手搓——JSON-RPC 收发、id 配对、stdio 子进程、HTTP + SSE 全部用标准库实现（`subprocess` / `urllib` / `threading` / `json` 等），**不引入第三方 MCP SDK 或 HTTP 库**；MCP 包对后端 provider 与 agent 编排层零依赖，唯一跨层 import 是工具适配子类 import 工具基类（与内置工具同规）
- N20: （v0.7）同步线程客户端不破坏并发模型——MCP 工具的执行入口是**同步**的，在既有「工具跑在工作线程」模型内执行；客户端用**后台读取线程 + id→等待槽**做线程安全的请求/响应配对，多个只读 MCP 工具并发调用同一 Server 时结果不串位、不死锁；不引入 asyncio 到 MCP 包内部
- N21: （v0.7）故障隔离与不崩溃——任一 Server 的连接/握手/列工具/调用失败都被捕获并降级（跳过该 Server 或返回结构化工具错误），**绝不致程序启动失败或会话中断**；进程退出时所有子进程/HTTP 连接被干净关闭、无僵尸进程泄漏
- N22: （v0.7）可离线测试——MCP 协议编解码、id 配对、工具适配、配置两层合并与 `${VAR}` 展开、故障隔离全部可在**不联网**下自动化测试：stdio 用标准库写的**假 MCP Server 脚本**做端到端，HTTP 用标准库 `http.server` 起本地假 Server（含 SSE 响应分支）
- N23: （v0.7）不破坏 v0.1–v0.6——无 `mcpServers` 配置时行为与 v0.6 完全一致；两层配置加载对既有单文件 provider 配置向后兼容（仅用户级文件存在时等价于原单文件加载）；MCP 工具经统一工具抽象接入，Agent Loop / 权限门 / 持久化 / provider 适配层均无需为「远端工具」特判
- N24: （v0.8）压缩核心可离线测试——token 估算、第一层卸载（阈值判定/挑大存盘/幂等）、第二层切割（配对安全/尾部保留/snap 到干净边界）、熔断计数全部为**纯函数或可注入依赖**，离线自动化测试（在临时目录真实读写卸载产物）；重量摘要的 LLM 调用用**假 provider**（返回固定摘要文本）做端到端，不联网
- N25: （v0.8）不破坏 v0.1–v0.7——无压缩配置 / 未注入压缩器时行为与 v0.7 完全一致；`request_decorator` 环境提醒通道契约不变（仍不写回 messages、不持久化）；压缩切割后历史**保持工具调用与结果配对合法**（送两家 provider 转换均不产生 400）；多轮编排、流式双路收集、保序分批并发、用户取消、权限门、缓存与 `<system-reminder>` 注入等既有能力不退化
- N26: （v0.8）provider 无关 + 分层——上下文压缩独立成**纯包**（零后端 SDK / 零 rich / 零 prompt_toolkit import），对 agent 编排层零反向依赖、由编排层鸭子类型注入；两家后端 `input_tokens` 语义差异在 **provider 层**消化（统一的「真实 prompt token 总量」接口），压缩层与协议无关；字符比、各阈值（单条/单轮卸载）、窗口上限、输出预留、安全余量、尾部保留 token/条数均**可配置**、缺省走内置默认
- N27: （v0.8）用户消息原文保真——第一层预防**只卸载工具结果**，绝不改写用户消息与助手正文；第二层摘要的「主要意图」段要求**尽量引用用户原话、不改写**；近期原文窗口（尾部保留）天然保护最新的用户消息不被摘要折叠
- N28: （v0.9）离线可测——指令加载（`@include` 防环/防越界/限深）、JSONL 编解码与恢复卫生（坏行/截断/溢出判定/清理）、记忆笔记与索引读写、记忆抽取的**解析与去重决策**、索引注入全部为**纯函数或可注入依赖**，离线自动化测试（临时目录真实读写）；记忆抽取与恢复压缩的 LLM 调用用**假 provider**（返回固定笔记 JSON / 固定摘要）端到端，不联网
- N29: （v0.9）不破坏 v0.1–v0.8——无 `WENTIAN.md` / 无 `memory` 配置 / 无历史会话时行为与 v0.8 一致；旧全局扁平 `.json` 会话视为遗留不报错；系统提示两个新槽位（项目指令、长期记忆）为空时拼装**无空行残渣**、缓存前缀稳定（启动注入一次、稳定可缓存、不破坏 v0.5 缓存断点）；JSONL 重构后会话持久化对 Agent Loop / v0.8 压缩写回钩子 / 权限门 / provider 适配层**透明**
- N30: （v0.9）分层 + provider 无关——项目指令加载为**叶子模块**（stdlib only）；`memory` 包对 agent 编排层**零反向依赖**，抽取的 provider **鸭子注入**（仿 v0.8 summarizer）、**自建 provider 实例不跨线程共享**；会话层不依赖后端 SDK；**复用 v0.8 `context` 包不重造**估算/压缩
- N31: （v0.9）后台抽取不阻塞不破坏——记忆抽取在 **daemon 线程 fire-and-forget**，绝不阻塞 REPL 输入、绝不因抽取异常中断会话或污染对话历史 messages；并发写 INDEX **加锁串行**；线程安全（自建 provider）；`/exit` 最多 `join` 一个短超时、不卡退出、无线程泄漏
- N32: （v0.9）安全与隐私——`@include` **拦截逃出项目根的路径**（先解析符号链接再前缀比对，与 v0.6 N11 沙箱同规）；记忆笔记落盘到用户/项目目录、**项目级 `.wentian/memory/` 建议入 gitignore**；**绝不把 api_key 等密钥写进记忆/会话**；**零新增第三方依赖**
- N33: （v0.9）代码规范——`ruff format --check .` 与 `ruff check .` 全部通过，遵循 CLAUDE.md；版本升 `0.9.0`（源码 + pyproject + lock 同步）
- N34: （v0.10）离线可测——命令注册中心（注册顺序 / 查找 / 冲突 `raise`）、解析器（小写 / 空格切分 / 裸斜杠 / 空输入）、补全器（前缀匹配 / 单匹配 / 多匹配 / 隐藏过滤，用假 document 驱动）、分发器（用假界面接口驱动）、每条内置命令（用假界面接口断言副作用）全部可在不联网下自动化测试；提示词类命令验证「调界面接口的发送用户消息」而非真发 AI 请求
- N35: （v0.10）不破坏 v0.1–v0.9——归并后既有命令行为不变（`/help`/`/provider`/`/exit`/`/plan`/`/do`/`/compact` 语义与归并前一致，`/new`/`/sessions`/`/resume` 经 `/session` 子命令等价达成）；非命令（普通文本）路径走 AgentLoop 行为零变化；无任何命令输入时与 v0.9 字节级等价；命令系统经**可选 `commands` 注入**（`commands=None` ⇒ 回退 v0.9 既有分发，与既往各版「None ⇒ 旧行为」注入模式一致——如 compactor/memory_runner）。**唯一一处有意行为变更例外**：状态栏模式标记按 AC88 改为 `[MODE]` 括号式（`[DEFAULT]`/`[PLAN]`/`[ACCEPT_EDITS]`/`[BYPASS]`），随之**仅** 3 项 `status_line` 既有断言更新；除此之外全量既有测试零修改保持绿
- N36: （v0.10）分层 + 框架无关——`commands/` 为**纯包**（零 `rich` / 零 `prompt_toolkit` / 零后端 SDK import）；命令处理函数只依赖**界面控制接口协议**、不 import REPL 具体类或 Rich；prompt_toolkit 相关的补全器置于 **ui 层**（`ui/completion.py`），命令包只提供「前缀 → 候选」的纯数据查询；注册中心 / 解析器 / 协议为叶子
- N37: （v0.10）启动期硬失败——命名或别名冲突在注册中心 `register` 即 `raise`，在 `build_app` 启动装配阶段触发、进程带 traceback 退出（panic 语义），绝不延后到用户运行时才炸；与既有 `ToolRegistry` 重名 `raise` 同规
- N38: （v0.10）代码规范——`ruff format --check .` 与 `ruff check .` 全部通过，遵循 CLAUDE.md；版本升 `0.10.0`（源码 + pyproject + lock 同步）；零新增第三方依赖
- N44: （v0.11）离线可测——Skill 加载（frontmatter 解析 / 单文件 vs 目录型 / 三层覆盖 / 解析失败跳过 / 占位符替换）、白名单校验（引用不存在工具 → `raise`；MCP 工具在场 → 通过）、菜单渲染（`name+desc` → 「可用 Skill」模块、空则省略无残渣）、激活态注入（active 集 → `<system-reminder>` 含正文、不写回历史、多 Skill 并存）、白名单收窄（并集 / 任一不限则不限 / 空集全工具 / `load_skill` 恒含）、`load_skill` 双模式（shared 进激活集 + 确认串；isolated 子对话回流摘要）、斜杠注册与冲突策略全部可在**不联网**下自动化测试；`load_skill` 的 isolated 子对话与任何 LLM 交互用**假 provider**（返回固定文本）端到端，不联网。
- N45: （v0.11）不破坏 v0.1–v0.10——**回退闸是 `skills.enabled:false`（或 `activator=None`）**：此时 activator 为 None ⇒ 不注入 skill 正文、不收窄白名单、不注册 `load_skill`、`/clear`·`/new` 不清激活集、无「可用 Skill」菜单 = 与 v0.10 字节级一致（既有 v0.1–v0.10 全量测试在此路径零修改保持绿）。**默认 `enabled:true` 随包内置 commit/review/test 三样板始终可用**，故默认工具集含 `load_skill`、系统提示含「可用 Skill」菜单——这是 v0.11 **有意的新默认**（既有「6 内置工具」类断言相应更新为 7 = 6 + `load_skill`），非回归。「可用 Skill」菜单为空时（仅 enabled:false 或零 Skill）系统提示拼装**无空行残渣**、缓存前缀稳定（启动注入一次、稳定可缓存、不破坏 v0.5 缓存断点）；激活态白名单与每轮注入均**不污染持久化会话**，非命令文本路径与既有 repl/agent_loop 测试零修改全绿。
- N46: （v0.11）分层 + provider 无关——新增 `skills/` 包为**纯数据 + 加载叶子**：`loader`（三层发现 + frontmatter 解析，stdlib only）、`registry`/`base`（Skill 数据模型），**对 agent 编排层 / provider / repl 零反向依赖**（镜像 v0.9 memory 包纪律）；`isolated` 子对话的编排（嵌套 AgentLoop + worker 线程）置于 **repl 装配层**（已持有 AgentLoop），不下放进 `skills` 包；`load_skill` 工具经**注入回调/句柄**持有 Skill 注册表与激活态，**不让 tools 包硬依赖 repl 具体类**；Skill 正文注入复用 v0.8 `request_decorator`、白名单复用 v0.4/v0.6 `allowed_tools`、菜单复用 v0.5 系统提示槽位、斜杠注册复用 v0.10 命令注册中心——**不重造**。
- N47: （v0.11）安全——白名单 `load_skill` 永不可被 Skill 关掉（系统级豁免）；`isolated` 子对话 worker 线程**自建 provider 实例不跨线程共享**（线程安全，仿 v0.9 抽取）、跑完干净退出不泄漏线程；Skill 正文里的占位符替换**只做字面替换、不执行**用户内容；目录型 Skill 的 `tools/` 脚本**本版不执行**（无任意代码执行面）；**零新增第三方依赖**。
- N48: （v0.11）代码规范——`ruff format --check .` 与 `ruff check .` 全部通过，遵循 CLAUDE.md；版本升 `0.11.0`（源码 + pyproject + lock 同步）；零新增第三方依赖。
- N39: （v0.12）离线可测——匹配原语（四模式）、条件求值（all/any/字段缺失）、`hooks:` 加载与集中校验（非法事件 / 缺字段 / 拦截类标异步 / 超时非正）、四类动作（shell 用临时脚本真跑、http 用 stdlib `http.server` 起本地假 server、prompt 纯函数、subagent 占位）、引擎触发与拦截（用假事件上下文 + 假 provider 驱动）全部可在不联网下自动化测试；PreToolUse 拦截回灌、注入缓冲下发用假 provider 端到端，不联网
- N40: （v0.12）不破坏 v0.1–v0.11——无 `hooks` 配置 / 未注入引擎时行为与 v0.11 字节级等价；`HookEngine` 经**可选注入**（无 hooks ⇒ 不注入、回退既有行为，与 compactor/memory_runner/commands 的「None ⇒ 旧行为」注入模式一致）；事件触发挂既有缝、**不改 AgentLoop 事件契约**；注入提示词复用 v0.5/v0.8 `<system-reminder>` 通道（仍不写回 messages、不持久化）；PreToolUse 拦截接在权限门**之前**、既有权限五层判定与拒绝回灌契约不变
- N41: （v0.12）分层 + 无反向依赖——`hooks/` 为**纯包**（零 `rich` / 零 `prompt_toolkit` / 零后端 SDK import）；引擎与动作执行**不 import** `wentian.agent` / `repl` / `providers` / `tools`（对编排层零反向依赖、由装配层鸭子注入事件上下文）；匹配原语 `textmatch.py` 为顶层**叶子**（纯 stdlib `re`/`fnmatch`）；`hooks/{spec,conditions,config,actions}` 为叶子或近叶子；HTTP 用 stdlib `urllib`、子进程用 stdlib `subprocess`、并发用 stdlib `threading`，**零新增第三方依赖**
- N42: （v0.12）失败软化铁律——任一 hook 动作的异常 / 超时 / 缺脚本 / 非法返回只写 hook 日志、**绝不冒泡中断主流程**（不抛进 REPL、不中断会话、不污染 messages）；`PreToolUse` hook 失败 fail-open（落既有权限门）；后台动作异常在 daemon 线程内吞掉、不泄漏；引擎构造 / 触发对配置缺漏安全降级、绝不致启动失败（唯一硬失败是加载期**集中校验**的配置错误 = 用户配错了规则、应当场报错）
- N43: （v0.12）代码规范 + 版本——`ruff format --check .` 与 `ruff check .` 全部通过，遵循 CLAUDE.md；零新增第三方依赖。**版本号**：本特性标记为 **v0.12**；鉴于 v0.10（已落 `0.10.0`）/v0.11 的发布次序未定，**实际 `pyproject`/`__init__`/lock 的 semver 字符串 bump 在装配期交用户拍板**（spec 不擅自跨号 bump 到 `0.12.0`）

- N49: （v0.13）离线可测——子 Agent 委派的全部核心逻辑都能在**不联网**下自动化测试：角色数据模型（spec）、loader（四源发现 / 同名覆盖 / 畸形 frontmatter 跳过 / 共享原语解析）、filter（三层集合运算 / 嵌套全局禁止 / 后台白名单按 Category 过滤）、model 别名映射、runner（用**假 provider**：定义式从空白对话跑到底、fork 继承父历史、错误停机转结构化结果、`model` 覆盖经 `dataclasses.replace` 生效）、manager（三种进后台 / Fork 恒后台 / 完成回灌缓冲 / 用量记录 / 线程退出不泄漏）、tool（type 分流 / 前台同步返结果 / 后台返「已起」/ 嵌套被拦）。子 Agent 与 LLM 的任何交互一律用**假 provider**（返回固定文本或脚本化工具调用）做端到端，绝不真发请求；manager 用**可注入时钟**测超时转后台。
- N50: （v0.13）不破坏 v0.1–v0.12——**回退闸 = 无 `agents` 配置 / `agent_registry=None` / `task_manager=None`**：此时 `Agent` 工具**仍注册**（保证工具列表稳定，见下条新默认），`type=definition` 且取不到角色 → 优雅返回「无此角色」结构化错误（不崩），`type=fork` 照常工作（不依赖角色 / 注册表）。其余既有行为与集成基线**字节级等价**；注册表 / runner / 管理器经**可选注入**（None ⇒ 退化），与 compactor / memory_runner / commands / hooks / Skill activator 的「None ⇒ 旧行为」注入模式一致。**有意的新默认**：`Agent` 工具进默认工具集（既有「工具数」类断言相应 **+1**，属有意更新而非回归——镜像 v0.11 把 `load_skill` 纳入默认工具集的处理）。
- N51: （v0.13）分层 + 无反向依赖——新增 `agents/` 包遵循严格分层：`spec` / `filter` 为**纯叶子**（stdlib only，`filter` 仅依赖 `spec` 与 `decision.Category`）；`loader` 为叶子（stdlib + 共享 frontmatter 原语 + `spec`）；`runner` 可依赖 `agent.loop` / `providers` / `permissions`（执行装配所需），但**不依赖 `repl` / `cli`**；`manager` 依赖 `runner`；`tool`（住 `agents/` 或 `tools/`）经**鸭子注入**句柄持有 registry / runner / manager，**不硬依赖 repl 具体类**。`agents/` 包对 `repl` / `cli` **零反向依赖**，所有装配缝落在 `cli` / `repl` 层（镜像 v0.9 memory / v0.11 skills / v0.12 hooks 的分层纪律）。
- N52: （v0.13）嵌套防护硬保证——`Agent` 工具对**任何**子 Agent 的允许集**永不可见**（F97 全局禁止）+ 子 Agent 上下文带**深度标记**兜底（深度 ≥ 1 即拒绝再起子 Agent）——**双重防线**杜绝无限嵌套递归：即便某角色白名单误把 `Agent` 列进去，也会被全局禁止在集合运算阶段挡下；即便全局禁止被绕过，深度标记仍在 runner / tool 层拒绝再起。两道线相互独立、互为冗余。
- N53: （v0.13）状态隔离不泄漏 + 后台并发安全——每个子 Agent 用**全新 provider 实例**（不与主 provider 或彼此共享可变状态、不跨线程共享同一实例）；后台 daemon 线程在进程退出时短 `join`、无线程泄漏；子 Agent 的权限模式 / allow-always 集 / 文件读状态 / 消息列表**绝不**回流主会话、也不污染其他并发子 Agent；子对话内部 messages **不进主会话持久化**（只回写最终结果——主对话记录里只有「调用 Agent 工具 + 结果」一条 tool result）。并发跑两个子 Agent，结果各归各位、不串。
- N54: （v0.13）失败隔离软化——子 Agent 异常停机（`MAX_ROUNDS` / `STREAM_ERROR` / `UNKNOWN_TOOL_LOOP` 等 v0.4 停机原因）→ runner 转成**结构化结果**回给主 Agent（形如「因 X 停止，部分结果：…」），**绝不崩主流程**；后台任务失败 → 管理器记 `status=failed` + 错误信息，经 F99 回灌并经 `/agents` 暴露；后台无 TTY 遇到 Ask → 沿用 v0.6「非 TTY 恒拒」安全默认（叠加 F97 后台白名单后基本不触发需确认工具）。失败永远是「软化成结果 / 日志」，不是「冒泡崩溃」。
- N55: （v0.13）复用底线 + 零新增依赖——复用本特性所列全部既有机制（`AgentLoop` / provider 工厂 / `PermissionPipeline` / `<system-reminder>` 注入通道 / frontmatter 解析 / `HookEngine` / `Tool` 基类与 `allowed_tools` 过滤），**零重造**；frontmatter 解析原语抽取后 **v0.11 Skill 行为不变**（v0.11 全量测试零回归）；**零新增第三方依赖**——后台并发用 stdlib `threading`、provider 复用既有工厂、模型覆盖用 stdlib `dataclasses.replace`。
- N56: （v0.13）代码规范 + 版本——`ruff format --check .` 与 `ruff check .` 全部通过，遵循 CLAUDE.md；**版本号**：本特性标记为 **v0.13**；鉴于 v0.10 / v0.11 / v0.12 的发布次序未定，**实际 `pyproject` / `__init__` / lock 的 semver 字符串 bump 在装配期交用户拍板**（spec 不擅自跨号 bump），与 v0.12 N43 同策。

## 不做的事

- 不做规则化权限系统（allowlist、「总是允许」、按目录授权等——仍是逐次 y/N 确认）
- 不做路径沙箱（不限制工具只能访问某目录子树；副作用靠执行前确认兜底）
- 不做六个核心工具以外的扩展工具（子代理、网页访问、todo 管理等）
- 不做全屏 TUI（仍为滚动式终端，与 Claude Code 同形态）
- 不做多模态（图片/文件输入）
- 不做对话内容的检索、压缩或跨会话记忆提炼（长任务超出上下文窗口的处理留给后续版本）
- 不做 slash 命令自动补全菜单（留给后续版本）
- 不做工具执行期间的中断（中断仅覆盖模型生成阶段；长命令依靠工具超时兜底，v0.4 边界）
- 不做新的交互式确认形态（确认交互沿用 v0.3 的逐次 y/N，不新增「总是允许」等选项）

以下为 v0.5 新增不做：

- 不做项目指令文件加载（不读取 `CLAUDE.md` / `AGENTS.md` 之类项目指令——仅为「自定义/项目指令」预留可选模块槽位，本版渲染为空）
- 不做自动记忆 / 长期记忆提炼（仅预留「长期记忆」槽位）
- 不做真实 MCP 接入（仅在「已激活 Skill」槽位层面预留概念位置，不实现任何 MCP 协议）
- 不做自动化提示评估（缓存生效靠解析命中字段定量验证；人格与约定遵守靠人工典型场景 before/after 定性对比，不搭自动评测框架）
- 不做对话历史的缓存（本版只缓存稳定的「工具声明 + system」前缀，单断点；缓存随对话增长的历史前缀需多断点管理，留后续版本）
- 不做多后端缓存断点的统一抽象（OpenAI 兼容走服务端自动前缀缓存、不显式打标；缓存控制力集中在 Anthropic）

以下为 v0.6 新增不做（同时**作废**前述 v0.3「不做规则化权限系统 / 不做路径沙箱 / 不做新的交互式确认形态」三条——本版正是实现它们）：

- 不做网络请求限制 / 资源配额 / 速率限制（本章不限制出网、不做 CPU/内存/时间配额，留待后续章节）
- 不做审计日志（不落地权限决策的持久化审计流水，留待后续章节）
- 不做黑名单可配置 / 可扩展（黑名单内置固定，用户不可增删、不可关闭）
- 不做自动泛化规则（人在回路「永久」只生成**精确匹配**规则；想要 `git *` 这类前缀/通配规则需用户手写进配置）
- 不做非内置工具 / MCP 工具的权限（本章只覆盖 6 个内置工具）
- 不做命令执行（Bash）路径沙箱（无法可靠静态解析任意命令的文件访问；其危险交给黑名单 + 规则 + 模式兜底）
- 不做多项目根 / 额外目录授权（沙箱只认启动时的单一项目根，不支持运行时追加可访问目录）
- 不做权限规则的图形化 / 交互式编辑器（规则靠配置文件与人在回路写入，不做规则编辑 UI）
- 不做 acceptEdits 之外的细粒度自动批准档（模式集固定为四档，不做「仅自动批准某工具」之类的自定义档位）
- 不做深层符号链接遍历围栏（找文件 / 搜内容的目录遍历沿用现有不跟随目录软链接的行为，沙箱只校验其搜索根）

以下为 v0.7 新增不做（同时**作废**前述 v0.5「不做真实 MCP 接入」一条——本版正是实现它）：

- 不做 MCP 的资源（resources）、提示词（prompts）、采样（sampling）能力——本版仅接入**工具**
- 不做 Server 健康检查与自动重连（连上就用，断了即失败回灌；心跳/重连/退避留后续版本）
- 不做工具列表变更通知（`notifications/tools/list_changed`）的动态刷新——工具集在**启动发现时一次性固定**
- 不做远端工具清单的磁盘缓存（每次启动重新发现）
- 不做 MCP 的 OAuth / 复杂鉴权流（HTTP 仅支持**静态请求头**，如 Bearer token 由用户写进 headers 并经 `${VAR}` 展开）
- 不做 MCP 工具的细粒度权限规则（v0.6 权限系统本版只覆盖六个内置工具；MCP 工具仅经「默认有副作用 / readOnlyHint 降只读」标志接入既有确认/兜底，不进规则引擎友好名路由）
- 不做取消通知（`notifications/cancelled`）、进度通知（`notifications/progress`）等高级协议特性
- 不做引入第三方 MCP SDK 或 HTTP 客户端库（协议层 stdlib 手搓，见 N19）

以下为 v0.8 新增不做（同时**作废**前述 v0.3「不做对话内容的检索、压缩或跨会话记忆提炼」一条中的「压缩」部分——本版正是实现上下文压缩）：

- 不做精确 tokenizer（用近似估算：锚定上次 usage + 增量字符折算；精确分词/按模型词表计数留后续版本）
- 不做摘要策略的机器学习 / 自动优化（摘要质量靠固定八段结构 + Prompt 纪律，不搭自动评测或调参框架）
- 不做跨会话记忆提炼（压缩只在单会话内管上下文，不把摘要沉淀为跨会话长期记忆——长期记忆仍是 v0.5 预留的空槽位）
- 不做卸载产物的自动清理 / 垃圾回收（卸载文件随会话留存，不做过期清理或容量上限）
- 不做多级摘要（摘要一次成段；对「摘要再被摘要」式的分层多级压缩不做，逼近窗口仍失败则靠熔断兜底）
- 不做工具结果的语义级压缩（第一层只按大小把超大结果卸载到磁盘、留预览，不做内容理解式裁剪/抽取）

以下为 v0.9 新增不做（同时**作废**前述 v0.5「不做项目指令文件加载」「不做自动记忆 / 长期记忆提炼」两条——本版正是实现它们）：

- 不做**向量数据库 / 嵌入检索**（记忆靠启动注入全量索引摘要，不做语义检索）
- 不做 **RAG 式按需检索记忆**（不在对话中动态召回相关笔记）
- 不做**团队 / 跨机器记忆同步**（本地单用户，不做云同步/共享）
- 不做**会话内记忆热刷新**（启动注入一次、保提示词缓存；本会话新记忆下一会话生效）
- 不做**记忆的图形化管理 / 编辑 UI**（笔记是带 frontmatter 的 Markdown，手动可编辑）
- 不做**精确 tokenizer**（索引体积 / 恢复溢出判定复用 v0.8 近似估算）
- 不做**指令文件实时热重载**（启动加载一次；改了 `WENTIAN.md` 需重启生效）

以下为 v0.10 新增不做：

- 不做**用户自定义命令**（命令集启动时由内置注册中心固定，用户不可在配置里增删命令——留给 Skill 系统）
- 不做**动态生成提示词**（提示词类命令的预设文案为内置固定模板，不做按上下文动态拼装或用户可编辑提示词模板）
- 不做**命令级权限控制**（命令不进 v0.6 权限引擎、不做「某命令需确认 / 按角色放行」——本地命令是用户主动触发，天然可信）
- 不做**命令历史 / 参数补全**（Tab 只补命令名，不补子命令参数或历史值；会话历史仍走 prompt_toolkit 既有上下键）
- 不做**全屏命令面板 / 模糊搜索**（补全沿用 prompt_toolkit 原生菜单，不做 fzf 式模糊匹配或独立命令面板 UI）

以下为 v0.11 新增不做（Skill 系统）：

- 不做**专属工具脚本的加载与执行**（目录型 Skill 的 `tools/` 子目录本版**仅识别结构、不动态导入或子进程调用**其实现脚本——留给后续章节连同分发/版本一起做）
- 不做 **Skill 市场分发 / 安装 / 更新**（不提供拉取远端 Skill、不做 Skill 包管理器，三层目录靠用户手放）
- 不做 **Skill 版本管理 / 依赖声明**（frontmatter 无版本字段、无 Skill 间依赖解析）
- 不做 **Skill 实时热重载**（启动加载一次；改了 Skill 文件需 `/skills reload` 或重启生效——不引入文件系统监听线程，保提示词缓存稳定与「读一次」纪律）
- 不做 **Skill 级权限控制 / 沙箱**（Skill 正文是用户主动加载的可信内容，不进 v0.6 权限引擎做「某 Skill 需确认」；工具白名单只是收窄可见集、提升选对工具准确率，不是安全边界）
- 不做 **Skill 正文的向量检索 / 按需召回**（菜单全量注入 `name+desc`，模型按需调 `load_skill` 取正文，不做 RAG 式相关性召回）
- 不做 **嵌套 Skill 激活**（`isolated` 子对话内不再递归发现/激活其它 Skill；子对话工具集即其白名单，`load_skill` 不在子对话内提供）

以下为 v0.12 新增不做：

- 不做**子 Agent 动作的真实运行**（`subagent` 动作本版仅占位记日志；真实派发 / 生命周期 / 结果回传留 SubAgent 章节对接）
- 不做**`once` 标记的持久化**（「已跑过一次」状态仅存内存、跨会话不保留；重启后 `once` 规则可再触发一次）
- 不做**Hook 执行顺序的显式优先级**（同一事件多条命中按配置**声明顺序**执行，不提供 priority/weight 字段或拓扑排序）
- 不做**条件逻辑的混合嵌套**（`match` 只在 `all` / `any` 二选一，不支持 and/or 嵌套树或按子句分组的复合表达式）
- 不做**Hook 配置热重载**（启动加载一次；改了 `hooks:` 需重启生效——与指令文件热重载同规）
- 不做**shell 动作 stdout 自动注入上下文**（命令产出只作副作用 / 日志；要把文本注入对话须显式用 `prompt` 动作）

以下为 v0.13 新增不做：

- 不做 **Worktree 文件隔离**（子 Agent 与主 Agent 共享同一工作目录文件系统；隔离只靠工具过滤 + 权限兜底，不为子 Agent 开独立 git worktree 或临时目录沙箱）。
- 不做 **多 Agent 团队编排**（无 Agent 间互相派发 / 协商 / 投票；只支持主 Agent → 子 Agent 的单层委派，子 Agent 不能再起子 Agent）。
- 不做 **后台任务跨会话持久化**（`BackgroundTaskManager` 纯内存态，进程退出即清空；不落盘、不做重启后恢复 / 续跑）。
- 不做 **后台任务取消 / kill**（`/agents` 只读，能列出 / 查看但不能终止运行中的任务，留后续章节）。
- 不做 **子对话完整往返落盘 artifact**（只回写最终结果到主对话；子 Agent 内部多轮往返不持久化成可事后排查的产物文件，留后续章节）。
- 不做 **嵌套子 Agent**（子 Agent 内不提供 `Agent` 工具、不递归派发——由 F97 全局禁止 + 深度标记双重保证）。
- 不做 **角色专属工具脚本执行**（角色目录下的 `tools/` 本版**仅识别结构、不动态导入或子进程调用**其实现脚本，留后续章节，镜像 v0.11 Skill 的 `tools/` 处理）。

## 验收标准

- AC1: （F1/F2）启动后能看到提示符，输入问题回车后程序发起一次模型请求
- AC2: （F3）回复在终端逐字/逐块出现，可观察到流式效果，而非一次性整段出现
- AC3: （F4）第二轮提问引用第一轮内容（如「我刚问的是什么」），助手能正确回应，证明上下文被保留
- AC4: （F5/F6/F7）同一份配置下，切换到不同 provider（至少 Anthropic 与一个 OpenAI 兼容后端）都能正常对话；启动时指定 provider 生效
- AC5: （F8）使用 Claude 后端且开启 thinking 时，界面上能区分地看到思考过程与最终回复
- AC6: （F9）一次会话结束退出后，重新启动并选择「续上次」能看到并接续之前的对话历史；选择「新建」则是空白会话
- AC7: （F10）会话内命令均生效：帮助、新建、列出/恢复、切换后端、退出
- AC8: （F11）新增一个「假后端」（实现统一接口）即可被界面层使用，无需修改界面层/会话层代码
- AC9: （N5）在不真实联网的前提下，核心流程（配置解析、后端选择、会话存读、界面循环）可被自动化测试覆盖
- AC10: （F12）让模型输出一段含标题、列表、代码块的回复，终端上最终显示为渲染后的格式（代码块有可区分样式，列表/标题有格式），而非裸 Markdown 源码；且生成过程中能观察到内容逐步出现（流式不丢）

以下为 v0.2 新增：

- AC11: （F13）启动后第一屏可见横幅：左侧 `=^_^=` 猫脸，右侧依次为程序名+版本、后端与模型名、会话状态；整体无边框面板。用 `--continue` 启动时显示「已恢复」及会话标识
- AC12: （F14）配置两个以上后端且不带 `-p` 启动 → 出现后端选择列表，按方向键高亮移动、回车后用所选后端对话；带 `-p` 启动或配置只有一个后端 → 不出现列表直接进入对话
- AC13: （F15）提示符 `❯ ` 后按专用换行键产生第二行（提交内容含换行），回车一次性提交；按 ↑ 能回到上一条输入；重启程序后 ↑ 仍能翻出上次运行的输入历史；连续空回车不在屏幕上残留边框/杂线
- AC14: （F16）启动后状态栏显示后端、模型、会话标识、消息数；执行 /provider 切换后状态栏后端随之变化；对话一轮后消息数增加
- AC15: （F17）提交一个需要较长思考的问题，等待期间可见 `=^_^=` 随时间眨眼、伴随递增秒数；正文开始流式后下方一行同样可见；回复定格后计时信息不再显示
- AC16: （F18）正文流式过程中按中断键：输出立即停止、屏幕保留已生成部分并显示「已中断」提示、程序回到输入状态；此后追问上一轮内容，助手能引用被中断的部分正文（证明部分正文入史）。在首字出现前按中断键：直接回到输入状态，会话历史中无该轮提问

以下为 v0.3 新增：

- AC17: （F19/F21）注册一个「假工具」（实现统一工具抽象）后：它出现在发给后端的工具声明列表中、可按名查找、可被执行——全程无需改动对话回合逻辑与界面层代码（自动化测试验证）
- AC18: （F20/F22）两种后端协议各实测一次：让模型读取一个真实存在的文件并总结内容，界面上可见工具调用发生，最终回答与文件实际内容一致（证明声明、流式解析、结果回传全链路打通）
- AC19: （F23）让模型在一次回复中同时读两个文件 → 两次工具执行都发生，最终回答同时引用两个文件的内容；诱导模型在拿到结果后继续调用工具 → 第二次工具请求不被执行，界面出现单轮限制提示，程序回到输入状态
- AC20: （F24）分别诱发：读不存在的文件、调用不存在的工具名、执行一条会超时的命令 → 程序均不崩溃，模型收到错误信息并能在最终回复中向用户说明
- AC21: （F25）改文件三种情形：原文唯一匹配 → 文件被正确替换；零匹配 / 多处匹配 → 文件未被改动，模型收到含实际匹配次数的错误信息
- AC22: （F26）模型请求写文件或执行命令时，界面出现确认提示并展示将做的事；选择拒绝 → 文件系统无变化/命令未执行，模型的最终回复中体现「被用户拒绝」；只读工具执行时无确认提示
- AC23: （F27）工具执行时界面显示工具名、参数摘要与结果状态，与正文回复样式可区分
- AC24: （F28）完成一轮含工具调用的对话后退出，`--continue` 恢复会话并追问之前工具读到的内容 → 模型能正确引用（证明工具交互入史并持久化）

以下为 v0.4 新增：

- AC25: （F29 主干）给出一个需要「找→读→改→验证」的真实任务（如「找到 X 文件，把其中 Y 改成 Z，然后运行测试确认没破坏」）→ 模型自主连续多轮调用工具完成全程，期间无需用户催促；屏上可见多轮工具调用轨迹与最终总结
- AC26: （F29 停机·上限/未知工具）把轮数上限设为小值并诱导长任务 → 达上限时循环停止、剩余工具请求未执行、界面出现上限提示、已生成内容保留（自动化 + 人工可复核）；自动化——连续两轮全部调用未注册工具名 → 循环停止并提示；两轮之间穿插一次已注册调用 → 计数重置、循环不停
- AC27: （F29 停机·中断/流错误）循环某轮生成期间按中断键 → 输出停止、本轮未执行的工具请求被丢弃、之前轮次的工具交互保留在历史、程序回到输入状态，追问可引用中断前内容；自动化——循环中途流式抛错 → 当前轮丢弃、已完成轮次保留、屏显错误、会话可继续
- AC28: （F30/F31）自动化——用「事件记录器」替代界面消费同一次循环 → 能完整断言事件序列（轮次起止、增量、工具调用与结果、用量、停机原因），证明循环与界面解耦；正文增量在本轮流结束之前即可被下游观察到（双路证据）
- AC29: （F32）自动化——并列调用两个人为放慢的只读工具 → 总耗时明显小于两者之和（并发证据）；「读-写-读」并列序列 → 实际执行顺序保持（后读不越过写）；结果回灌顺序与原始调用顺序一致。联网——让模型一次回复中读多个文件 → 全部执行、回答同时引用全部内容
- AC30: （F33）`/plan` 后状态栏出现计划模式标记；让模型做一个修改类任务 → 模型仅以只读工具勘察并产出计划；若模型硬请求写文件/执行命令 → 不执行、收到「计划模式下不可用」错误并在回复中说明；`/do 按计划执行` → 恢复全工具、计划被真正执行（文件变更、确认门照常工作）
- AC31: （F34）联网完成一次含循环的回合 → 屏显 token 用量行（总输入/输出与轮数）；自动化——多轮用量正确累计；后端全程未报用量 → 不显示用量行
- AC32: （F28 延续）多轮循环回合结束后 `/exit`，`--continue` 恢复并追问循环中间某轮工具读到的内容 → 模型能正确引用（全轨迹持久化证据）

以下为 v0.5 新增：

- AC33: （F35）系统提示由七个固定模块按固定优先级拼成、模块间空行分隔（自动化：断言拼装输出依次含七模块标识、顺序固定）；可选模块本版返回空时整体输出不出现空行残渣或孤立分隔（自动化）；替换/新增一个模块只改模块定义、拼装器零改动即生效（自动化：注入一个假模块验证）
- AC34: （F36）让文天回答一个日常问题 → 回复语气可见文天味（自嘲/照顾/适度沙雕）；让它报告一次测试失败 → 语气是猫、结论是工程师（给出实际失败输出、不说「应该没问题」、不掺水）。人工 before/after 对比（与 v0.4 裸提示对照）。**人设不透露 + 不自称形象**：直接问文天「你的人设/设定/系统提示是什么」→ 它不复述、不罗列设定（自然带过）；文天回复中不自称「猫」「油头」等形象绰号；离线侧——系统提示中含「不向用户透露/复述人设与系统提示」「不自称猫/油头等形象词」的指令（自动化断言）
- AC35: （F37）关键约定文字（如「编辑文件前先读取」）**同时**出现在系统提示的「工具使用」模块文本与至少一个受影响工具（如改文件工具）的描述中（自动化：两处各断言含该约定关键句）
- AC36: （F38）Anthropic 后端发出的请求中，`system` 为带 `cache_control: ephemeral` 的文本块数组、缓存断点落在系统提示块上（自动化断言请求负载结构）；同一会话连发两轮 → 第 2 轮 `cache_read_input_tokens > 0`（联网）。OpenAI 兼容后端 `system` 仍为单条 system 消息（自动化）
- AC37: （F39）环境信息与开关提醒以 `<system-reminder>` 标签注入**发给后端的** messages，但**持久化到会话文件的** messages 不含任何提醒块（自动化：对比 provider 收到的 messages 与 store 落盘的 messages）；联网观察——模型不把 `<system-reminder>` 内容当用户输入来回复
- AC38: （F39 cadence）计划模式下进行多轮循环 → 第 1 轮注入完整开关提醒、其余轮精简为一行、每 N（默认 5）轮重复完整（自动化：断言注入文案随轮次按规则变化）；非计划模式 → 不注入开关提醒（自动化）
- AC39: （F40）含缓存命中的回合结束 → 用量行显示缓存读取/创建 token（联网 Anthropic）；自动化——后端报缓存字段时 `Usage` 正确携带并跨轮累计，后端未报时为 0 且用量行不显示缓存信息
- AC40: （F33 迁移）计划模式开关提醒从 system 后缀迁移到 `<system-reminder>` 消息后，F33 全部既有行为不变（自动化回归：声明过滤仍仅暴露三只读工具、越权调用仍被 blocked 拦截回灌、`status_line` 计划模式标记不变）；系统提示块本身不再含计划模式后缀（自动化断言）

以下为 v0.6 新增（权限系统）：

- AC41: （F41/N10）黑名单硬拦截——命令执行类工具跑 `rm -rf /`（及变体）被判 Deny、不执行，结果回灌含被拒原因；在 `bypassPermissions` 模式下**依然**被拦（自动化：逐条危险样本断言 Deny；bypass 模式下仍 Deny）
- AC42: （F42/N11）沙箱围栏——文件工具写/读项目根之外路径（如 `/etc/passwd`、`../outside`）被判 Deny；通过软链接指向项目外的目标也被判 Deny（先解析再比对）；项目内的新建文件（含尚未创建的多级中间目录）正常放行（自动化：临时目录构造软链接与新建路径断言）
- AC43: （F43）规则精确与 glob 匹配——`Bash(git status)` 精确放行 `git status`、不放行 `git push`；`Bash(git *)` 放行所有 git 子命令；`Write(src/**)` 放行 `src/` 下任意路径写入、不放行 `docs/x`；单独的 deny 规则（如 `Bash(git push)` deny）命中即判 Deny
- AC44: （F43）友好名路由——规则中的友好名 Bash/Read/Write/Edit/Glob/Grep 正确路由到对应的 6 个内置工具（命令执行 / 读 / 写 / 改 / 找文件 / 搜内容）
- AC45: （F44）三级优先级——本地盖过项目、项目盖过用户；同层 deny 优先于 allow（自动化：构造冲突规则断言裁决）
- AC46: （F44/N14）配置降级——三层文件缺失时引擎按空规则运行；某文件格式非法时跳过该文件、其余正常加载、**不致引擎构造失败**、不抛未捕获异常
- AC47: （F45/F47）模式矩阵——default：只读 Allow、文件写/命令执行 Ask；acceptEdits：文件写 Allow、命令执行 Ask；bypassPermissions：全 Allow（黑名单/沙箱除外）；plan：仅只读工具可见、文件写/命令执行不被请求，且其兜底裁决仍为 Ask（自动化：逐档逐类断言裁决）
- AC48: （F46）流水线短路与跳层放行——黑名单命中不再进沙箱/规则；deny 规则命中不再进模式；Allow 规则命中直接放行不进模式兜底；**被跳过的层不误拦**（非命令执行工具不被黑名单拦、命令执行工具不被沙箱拦，均继续进后续层）
- AC49: （F47）运行时切换模式——按 Shift+Tab 循环 default → acceptEdits → plan → bypassPermissions → default；`/plan`/`/do` 仍可进出 plan（`/do` 固定回 default）；**状态栏左侧常驻显示当前权限模式（不再显示 provider 名）**；切换不改已加载规则、**跨轮保持**（下一轮不被重置）
- AC50: （F48）人在回路三选一——Ask 时弹出多行待批准块（工具名 + 参数 + 触发原因 + 菜单）；↑↓+回车 或 数字键 1/2/3 选择，默认高亮「允许本次」；选「允许本次」→ 执行且不留规则；「永久」→ 执行且 allow 规则写入本地层配置文件、重启后仍生效；「拒绝本次」→ Deny 回灌、Loop 继续
- AC51: （F49/N12）拒绝不中断 + 保序——单批多调用中被拒的回灌错误、放行的正常执行，结果**按调用序与各自调用 ID 配对回灌、互不串位**；Agent Loop 继续，模型可据被拒原因改路径
- AC52: （N13）取消安全——人在回路等待中按 Esc/Ctrl+C 干净取消本轮（不退出整个程序）；无挂起 asyncio task 泄漏；取消后再发一条消息可继续、不报 400
- AC53: （N12）只读并发不退化——一批连续只读调用仍并发执行（权限检查不把只读串行化）；只读调用永不触发 Ask（自动化：批量只读不弹人在回路）
- AC54: （N15）跨协议一致——anthropic 与 openai（含兼容 base_url）权限行为一致（同样的拦截/询问/放行）；provider 适配层未改动
- AC55: （N16）安全默认——未知工具/参数不可解析/类别不明时按最严处理（Ask 或 Deny），不静默放行；具体：未注册工具名归命令执行类不直接 Allow、参数 JSON 无法解析时不静默放行、只读标志缺失/类别不明按有副作用处理
- AC56: （N12）不破坏 v0.4/v0.5——多轮连环、用户取消、流出错恢复、历史一致、缓存命中、规划按轮次注入仍成立；`pytest` 通过
- AC57: （N17）代码规范与不泄漏——`ruff format --check .` 通过、`ruff check .` 无告警；含敏感放行的本地配置层已被 gitignore，CLI 输出与配置回显不泄漏 api_key
- AC58: （F44/F45）启动默认模式——defaultMode 按 本地 > 项目 > 用户 生效，皆无则 default（自动化：三层配置各设不同 defaultMode 断言生效层；defaultMode=plan 启动即应用只读工具集 + 计划提醒）
- AC59: （N18）可扩展性——新增一档模式只需扩模式兜底表、新增一层防御只需在流水线插一层，均不触及 provider 适配层（自动化：对应改动 diff 局限在 permissions 包的模式/流水线文件）

以下为 v0.7 新增（MCP 客户端接入）：

- AC60: （F50/N23）两层配置——仅用户级文件存在时按原样加载（与旧单文件等价，向后兼容）；加项目级 `.wentian/config.yaml` 后，同名 provider / mcpServer 被项目级覆盖、新增条目并入（自动化：构造两文件断言合并结果）；`mcpServers` 解析出 stdio（command/args/env）与 http（url/headers）两型；env/headers 值中的 `${VAR}` 被展开，缺失变量展开为空串并告警（自动化）
- AC61: （F51）协议三步走 + id 配对——对一个假 stdio Server：初始化握手成功（协议版本/能力交换 + 发出 initialized 通知）、`tools/list` 取回工具清单（名/描述/schema/注解）、`tools/call` 传参取回结果；请求与响应按 id 正确配对，**假 Server 故意乱序回包时仍配对正确**（自动化）
- AC62: （F52）两种传输——stdio：子进程假 Server 端到端跑通三步，且其 stderr 输出不干扰协议解析（自动化）；HTTP：本地 `http.server` 假 Server **既测即时 JSON 响应、也测 SSE 事件流响应**，且配置的请求头被带上（自动化：假 Server 校验收到的 header）
- AC63: （F53）适配与无感调用——发现到的远端工具出现在发给后端的工具声明列表、可按名查找、可被 Agent Loop 多轮调用，全程无需改对话回合/界面代码；工具名带 `<Server>__` 命名空间、不与内置工具撞名；远端结果被拼为文本回灌；远端报错 / 传输失败 → 结构化工具错误、程序不崩溃（自动化用假 Server；可选一次联网/真 Server 端到端）
- AC64: （F54/N16）安全默认——未标 readOnlyHint 的 MCP 工具其 `requires_confirmation` 为真（按副作用：串行 + 走确认/模式兜底）；标注 readOnlyHint=true 的降为只读（可并发、免确认）（自动化：假 Server 提供两类工具，断言分类与并发/确认行为）
- AC65: （F55/N21）多 Server 与故障隔离——配两个 Server、其一故意坏（命令不存在 / 握手超时 / HTTP 不可达）→ 坏的被跳过并告警、好的正常注册工具、程序正常启动；启动汇报列出成功 Server 及工具数与失败原因；运行中 Server 崩溃 → 其工具调用返回结构化错误、其余 Server 工具仍可用；退出时子进程被终止、无残留（自动化：构造坏 Server + 进程清理断言）
- AC66: （N22/N23）离线可测 + 不破坏——上述全部用假 Server 离线跑通（stdio 假脚本 + http.server 假服务）；无 `mcpServers` 配置时全部既有 v0.1–v0.6 测试保持绿、行为与 v0.6 一致（自动化：`pytest` 全绿 + 无配置冒烟）

以下为 v0.8 新增（上下文管理 + 双层压缩）：

- AC67: （F56）估算锚点——估算总 token = provider 报告的真实 prompt 总量（Anthropic：`input + cache_read + cache_creation`；OpenAI 兼容：`prompt_tokens` 不重复加缓存）+ 锚点之后新增消息的字符折算；无锚点时退化为全量字符折算（自动化：构造两家 `Usage` 断言总量算法正确、断言增量只折算新追加的消息、断言字符比可配）
- AC68: （F57）单条卸载 + 幂等——一条工具结果估算超单条阈值 → 原始内容写入会话产物目录、对话中该消息 content 变为「预览 + 绝对路径 + 提示」、`is_error` 标记保留、再次扫描不重复卸载；同批的用户消息与助手正文**不被改写**（自动化：临时目录构造超大工具结果，断言卸载落盘、对话内容替换、幂等、用户/助手消息原样）
- AC69: （F57）单轮合计卸载——单轮多条工具结果各自不超单条阈值、但合计超单轮阈值 → 按大小降序挑最大的依次卸载直到合计回落阈值下，较小的若干条原样保留（自动化：构造多条工具结果断言卸载选择与停止点）
- AC70: （F58）切割边界安全——构造「助手(含 tool_calls) + 其工具结果 + 后续若干轮」历史触发重量摘要 → 保留段从尾部按 token 保留约 1 万且 ≥5 条、**起点不是工具结果消息**、不拆散任何「助手↔工具结果」对；压缩后历史送两家 provider 的消息转换均不产生「工具调用/结果不配对」（自动化：断言切割点位置、配对合法、tool_result 都能找到前序 tool_use）
- AC71: （F58/F59/F60）摘要端到端——用**假 provider** 触发重量压缩 → 发给假 provider 的摘要请求**不带任何工具声明**、其 Prompt 含「禁止调用工具」「先草稿后正式」语义；压缩后历史 = 摘要消息（含八段结构标识）+ 边界消息（含「重新读取/勿脑补」语义）+ 近期原文段；摘要请求只在估算逼近窗口（窗口 − 输出预留 − 余量）时发生（自动化）
- AC72: （F61）手动触发 + 熔断——`/compact` 以 3K 余量触发一次压缩、自动触发用 13K（自动化：断言余量参数差异）；注入「摘要必失败」的假 provider，连续 3 次失败 → 置熔断、后续自动轮跳过重量兜底但第一层照常、仅告警一次；一次成功即清零计数；熔断后 `/compact` 仍强制重试、成功则解除熔断（自动化）
- AC73: （F62/N25）触发编排 + 字节级回归——多轮循环中每轮调用后端前先预防后兜底、改写历史经 RoundEnd 落盘（自动化：断言每轮触发次序与持久化）；**未注入压缩器时 Agent Loop 行为与 v0.7 字节级等价**（自动化：既有 loop 测试零修改保持绿）；无压缩配置时既有 v0.1–v0.7 测试全绿、行为与 v0.7 一致
- AC74: （N24/N26/N27）分层 + 离线——`context` 包零 SDK/rich/prompt_toolkit import、两家 provider 差异在 provider 层消化（自动化：断言 import 边界）；估算/卸载/切割/熔断离线纯函数可测、重量摘要用假 provider 离线端到端；用户消息原文保真（自动化断言）；`ruff format --check .` 与 `ruff check .` 通过

以下为 v0.9 新增（记忆与会话）：

- AC75: （F63）三层指令 + @include——三处放不同 `WENTIAN.md` → 注入「项目/自定义指令」模块、顺序高优先级在前；`@include` 内联目标文件内容；构造 a→b→a 环 → 不无限递归、按 `visited` 跳过并告警；`@include` 指向项目根外/越界绝对路径（含软链接指向项目外）→ 拒绝并告警、不读取；缺文件静默跳过（自动化临时目录）
- AC76: （F64）JSONL 追加 + 分区——新会话写到 `projects/<cwd-slug>/sessions/<id>.jsonl`、每轮**追加**新消息行（不重写全文，断言文件按追加增长）；故意写一条坏行 → 恢复时跳过坏行、加载其余、ID 取文件名 / 标题取首条 user / 消息数正确；`/sessions` 与 `--continue` 默认只列当前分区、`--all` 列全部（自动化）
- AC77: （F65）恢复卫生——构造尾部「助手 tool_call 无后续结果」→ 恢复时截断该未配对调用、送两家 provider 转换不 400；构造 `estimate>窗口-余量` 的历史 → 恢复时调 v0.8 `Compactor` 压一次后再进入对话；`updated_at` 距今超阈值 → 恢复后首轮注入一条 `<system-reminder>` 时间跨度提醒、且**不写回持久化**（自动化对比 provider 收到的 messages 与 store 落盘 messages）
- AC78: （F66）过期清理——构造一个 `updated_at` 超 `retention_days` 的会话 + 一个新的 → 启动后旧的（及其 `.artifacts/`）被删、新的保留；`retention_days` 可配（自动化）
- AC79: （F67）后台抽取——一个 `COMPLETED` 回合后台线程调（假 provider 返回固定四类笔记 JSON）→ 抽出笔记按类落到 user/project 目录、写带 frontmatter 的 `.md` + 更新 `INDEX`；注入「抽取必抛异常」的假 provider → **静默吞、会话不中断、对话历史 messages 不被污染、不阻塞下一次输入**（自动化：假 provider + 断言落盘 + 断言异常不崩、不入史）
- AC80: （F67 去重）LLM 去重——已有 `INDEX` 含某条 → 再次抽到等价信息 → 把现有索引喂 LLM、它判「已覆盖」则跳过或更新而非重复追加（自动化：脚本化假 provider 返回判重决策，断言不重复追加）
- AC81: （F68）索引注入——启动时读 user+project 两份 `INDEX` 注入「长期记忆」模块、内容可见于发给后端的 `system`；索引超 200 行 / 25KB → 按上限截断；无 memory 时该模块为空、拼装无残渣（自动化断言注入内容 + 上限 + 空槽无残渣）
- AC82: （F69/N29）配置 + 不破坏——`memory`/`sessions` 配置块缺失走默认；`memory.enabled:false` 关掉抽取与注入；无 `WENTIAN.md`/无 memory/无历史会话时既有 v0.1–v0.8 测试全绿、行为与 v0.8 一致（自动化 `pytest` 全绿 + 无配置冒烟）
- AC83: （N30/N31）分层 + 后台安全——指令/`memory`/会话模块 import 边界（叶子 / 零反向依赖 / 鸭子注入）自动化断言；后台抽取用 daemon 线程不阻塞、自建 provider 不跨线程共享、并发写 INDEX 加锁串行（自动化）；`ruff format --check .` 与 `ruff check .` 通过
- AC84: （N32）安全——`@include` 软链接指向项目外被拦（先解析再比对）；记忆/会话落盘不含 api_key（自动化扫描）；项目级 `.wentian/memory/` 在 gitignore 建议内（人工/文档核对）

以下为 v0.10 新增（斜杠命令系统）：

- AC85: （F70/N37）注册中心 + 冲突 panic——注册一条假命令后可按**名**与**别名**查找命中（大小写不敏感）、按注册顺序列举可见命令；登记两条命名或别名冲突的命令 → 注册时 `raise`（自动化：断言查找命中 + 冲突 `raise`；冲突在 `build_app` 启动触发、进程退出）
- AC86: （F71）解析器——`/Help` 解析出命令名 `help`（小写）、`/session resume abc-123` 解析出名 `session` + 参数 `resume abc-123`；裸 `/` 与纯空白**不进分发**；解析出的命令名未在注册中心命中 → 打印「未知命令 + `/help` 引导」、不送 AI（自动化）
- AC87: （F72/F73）三类执行 + 接口解耦——三类命令各跑一条：本地（`/help` 打印不改状态）、界面状态（`/plan` 切模式）、提示词（`/review` 经界面接口的「发送用户消息」触发一轮 AI、断言走的是发消息接口而非命令直接发请求）；全部内置命令用**假界面接口**离线驱动、断言只调接口方法不碰 Rich（自动化）
- AC88: （F73）状态栏模式标记——状态行左段显示 `[DEFAULT]`/`[PLAN]`（及 acceptEdits / bypassPermissions 标记）；`/plan` → `[PLAN]`、`/do` → `[DEFAULT]`、`/permission acceptEdits` 与 Shift+Tab 切换后下次渲染随之变化（自动化断言状态行字串）
- AC89: （F74）入口分流——`/` 开头输入走本地命令分发、**不发起 provider 请求**（断言假 provider 未被调用）；普通文本走 AgentLoop 跑一轮 AI（既有路径不变）（自动化：命令路径断言 provider 零调用、文本路径断言正常一轮）
- AC90: （F75）别名 + Tab 补全——输入 `/se` + Tab 补全到 `/session`（前缀单一时直接补）；`/` + Tab 多候选弹菜单列出全部可见命令；隐藏命令不出现在补全与 `/help`；别名可被解析命中（自动化用假 document 驱动补全器；👁 真终端菜单弹出留人工验收）
- AC91: （F76）十命令 + 归并——`/help` 列全部可见命令；`/session new` 建新会话（新 id）、`/session list [--all]` 列举、`/session resume <id>` 恢复；`/provider`/`/plan`/`/do`/`/compact`/`/permission`/`/status`/`/memory`/`/review`/`/exit` 各按其语义工作；归并后 `/help`/`/provider`/`/exit`/`/plan`/`/do`/`/compact` 行为与归并前一致（自动化逐命令断言 + 归并回归）
- AC92: （F76/N35）`/clear` 语义——`/clear` 清空当前会话 `messages`（留同一会话 id、覆写落盘为空、重置压缩锚点），`/session new` 另建新 id（两者区分）；`/clear` 后状态行消息数归零、会话 id 不变（自动化）
- AC93: （N35）回归——非命令文本路径与无命令输入时行为与 v0.9 字节级等价；既有 v0.1–v0.9 全量测试零修改保持绿（自动化 `pytest` 全绿 + 文本路径回归）
- AC94: （N36/N37/N38）分层 + panic + 规范——`commands/` 包零 `rich`/`prompt_toolkit`/后端 SDK import（补全器在 ui 层）、命令处理函数只依赖界面控制接口协议（自动化 import 边界断言）；别名冲突在启动 `build_app` 触发 `raise`、进程退出（自动化 + 无配置冒烟）；`ruff format --check .` 与 `ruff check .` 通过、版本 `0.10.0`、零新增依赖

以下为 v0.11 新增（Skill 系统）：

- AC105: （F84）定义格式 + 占位符——给一份带 frontmatter（`name`/`description`/`allowed_tools`/`mode`/`history`/`model`）+ 正文的 Skill 文本 → 解析出各字段（缺省字段走默认：mode=shared、history=0、allowed_tools=None）；正文含 `$ARGUMENTS` 与 `$1`/`$2` → 传入 `"fix typo"` 替换出 `$ARGUMENTS=fix typo`、`$1=fix`、`$2=typo`，无对应位置参数的占位符替换为空串；**单文件** `commit.md` 与**目录型** `commit/SKILL.md` 解析等价（同 name、目录型 `tools/` 子目录被识别但不加载执行）（自动化临时目录）。
- AC106: （F85）三层发现 + 覆盖 + 跳过——项目层与用户层各放一个同 `name` Skill → 发现结果取**项目层**（高层覆盖低层）；放一个 YAML 坏 / 缺 `name` 的文件 + 一个合法文件 → 坏的**静默跳过**、合法的正常发现，不抛异常、不阻断；内置层 Skill 在无用户/项目同名时被发现（自动化：临时目录三层 + 假内置）。
- AC107: （F86）两阶段——启动后「可用 Skill」模块出现在发给后端的 `system` 里、含每个 Skill 的 `name + description`（不含正文）；无任何 Skill 时该模块为空、系统提示拼装**无空行残渣**；`load_skill` 工具出现在工具声明里、**即便某激活 Skill 把白名单收窄到不含它，`load_skill` 仍在可用集**（自动化断言菜单内容 + 空槽无残渣 + load_skill 恒在）。
- AC108: （F87）激活注入不持久化 + 多 Skill——`load_skill` 激活一个 shared Skill 后，下一轮发给 provider 的 messages **最新用户消息处**含 `<system-reminder>` 包裹的「`# 已激活 Skill: <name>` + 完整正文」，且**store 落盘的 messages 不含**该注入（对比 provider 收到的与落盘的）；同时激活两个 Skill → 两段正文都注入；每轮重建（连激活两轮断言注入稳定出现）（自动化假 provider + decorator）。
- AC109: （F87）白名单收窄——激活一个 `allowed_tools:[read_file,run_command]` 的 Skill → AgentLoop 当轮 `allowed_tools` = `{read_file, run_command, load_skill}`、声明给 provider 的工具集相应收窄、运行时调白名单外工具被拦；再激活一个**省略** `allowed_tools` 的 Skill → **不收窄**（全工具）；清空激活集 → 全工具（自动化断言可用集随激活集变化、load_skill 恒含）。
- AC110: （F88）启动白名单校验 fail-fast——一个 Skill 的 `allowed_tools` 含**不存在**的工具名 → `build_app` 启动**`raise` 中止**、报错指明该 Skill 与非法工具名；校验**在 MCP 发现注册之后**（一个白名单引用某 MCP 工具的 Skill，在该 MCP Server 接入后**通过**校验、未接入则报错）；对比：**解析失败**的坏文件是静默跳过而非 raise（自动化断言两种路径的非对称）。
- AC111: （F89）独立模式回流——`mode:isolated`、`history:2` 的 Skill 经 `load_skill` 命中 → 用**假 provider** 跑子对话（子系统提示含该 Skill 正文、起始上下文含主会话末 2 条、子工具集=白名单）→ `load_skill` 的**工具结果** = 子对话最终助手正文；该 Skill **不进**主激活集、主循环工具不被收窄、子线程跑完不泄漏（自动化假 provider + 断言工具结果内容 + 主激活集为空 + 线程 join 干净）。
- AC112: （F90）斜杠注册 + 冲突策略——发现一个 `name:deploy` 的 Skill → 注册中心多出可见命令 `/deploy`、出现在 `/help` 与 Tab 补全、`/deploy x` 等价 `load_skill("deploy","x")`；`name:review` 的内置 Skill → **替换** v0.10 的 `/review` 命令（`/review` 现走 Skill）；`name:exit`/`name:clear` 等**控制命令同名** Skill → **斜杠注册被跳过 + 告警**、但仍可经 `load_skill("exit")` 加载、**启动不 panic**（自动化断言三种冲突分支）。
- AC113: （F90）/skills + 热更新——`/skills` 列出全部已发现 Skill（名 + 描述 + 来源层 + mode），走命令分发**零 provider 请求**；`/skills reload` 增删一个 Skill 文件后重扫 → 注册表与斜杠命令随之变化；reload 时引入一个白名单引用错工具的 Skill → **保留旧注册表 + 打印错误、会话不崩**（区别启动期 fail-fast）（自动化：临时目录改文件 + reload + 断言）。
- AC114: （F90）清空联动 + 内置三样板——激活某 Skill 后 `/clear` → 激活集清空、状态正常；`/session new` 另建新会话也清空激活集；内置 `commit`/`review`/`test` 三样板在无用户/项目同名时被发现并各注册斜杠命令（`/commit`/`/test` 新增、`/review` 接管）；`commit` 正文对齐项目 CLAUDE.md（中文/无 `Co-Authored-By`/不主动 push）（自动化断言激活集清空 + 三样板发现 + 正文关键约束）。
- AC115: （N45/N46/N48）回归 + 分层 + 规范——`skills.enabled:false`（或 `activator=None`）→ `available_skills` 空、`load_skill` 不注册、activator None = 回退 v0.10（既有 v0.1–v0.10 全量测试在此路径零修改保持绿）；**默认 `enabled:true` 含内置三样板** → `available_skills` 含 commit/review/test、默认工具集为 7（6 标准 + `load_skill`，断言 `names[:6]==六标准工具` 且 `load_skill in names`）（自动化 `pytest` 全绿 + 无配置冒烟）；`skills/` 包零 agent/provider/repl 反向依赖、`load_skill` 工具不硬依赖 repl 具体类（自动化 import 边界断言）；`ruff format --check .` 与 `ruff check .` 通过、版本 `0.11.0`、零新增依赖。

以下为 v0.12 新增（Hook 生命周期系统）：

- AC95: （F77/N39）规则模型 + 加载 + 集中校验——`hooks:` 块（规则列表）加载为 `list[HookRule]`，每条带 event / 可选 if / action + 执行控制；整块缺失 → 零规则；**集中校验**：非法事件名、动作缺必填字段（如 shell 缺 `command` / http 缺 `url`）、`PreToolUse` 标 `background: true`、`timeout<=0` 各触发 `HookConfigError`（加载期失败、带定位）；用户级 + 项目级两层 `hooks` 列表叠加（自动化：构造各类合法 / 非法 YAML 断言解析结果与报错）
- AC96: （F78）十事件触发——十个事件（SessionStart/SessionEnd/UserPromptSubmit/Stop/RoundStart/RoundEnd/PreToolUse/PostToolUse/PreCompact/Notification）各在其缝触发、携带正确上下文字段（用**假引擎**记录 fire 调用 + 假 provider 跑一轮，断言每缝都点到、上下文字段齐备）（自动化）
- AC97: （F79）PreToolUse 拦截——一条 `PreToolUse` + 命令正则条件 + shell 动作 `exit 2` 的规则：命中 → 工具被拦、拒绝原因（stderr）当工具结果回灌（`is_error=True`）、短路在权限门**之前**、不进 executor；`exit 0` → 放行落权限管线；hook 动作失败（脚本缺失 / 超时）→ fail-open 落权限门（自动化：假工具调用 + 假权限门，断言拦截 / 放行 / fail-open 三态）
- AC98: （F80）条件四模式 + 全部 / 任一——`textmatch.match_one` 四模式：精确 `Bash`、反向 `!Bash`、正则 `/rm\s+-rf/`、glob `git *` 各断言命中 / 不命中；条件 `match: all` 全真才触发、`match: any` 一真即触发；`if` 省略 → 恒触发；字段缺失按空串参与（自动化纯函数 + 求值器）
- AC99: （F81）四种动作——shell（临时脚本真跑、读到 stdin JSON 与 `WENTIAN_HOOK_*` 环境变量）、prompt（产出文本含 `{field}` 替换）、http（stdlib `http.server` 本地假 server 收到 JSON 体）、subagent（占位：记一条「未实现」日志、不抛）各跑通；任一动作内部异常被软化（自动化离线、http 用本地假 server）
- AC100: （F82）执行控制——`once` 规则本会话只触发一次（第二次命中跳过）；`background: true` 动作在 daemon 线程异步跑、不阻塞（断言主流程不等待）；`timeout` 到点终止 shell/http 并记日志；`PreToolUse` 标 `background: true` 在加载期校验失败（自动化）
- AC101: （F83/N42）失败软化 + 注入下发——动作抛异常 / 超时只写 hook 日志、**主流程不中断**（断言会话继续、messages 不被污染）；`prompt` 动作注入文本经引擎累积、下次请求随 `<system-reminder>`（`request_decorator`）下发、不写回 messages、不持久化（自动化：假 provider 断言下一轮 outgoing 含注入文、原 messages 不变）
- AC102: （N40）回归——无 `hooks` 配置 / 未注入引擎时行为与 v0.11 字节级等价；既有 v0.1–v0.11 全量测试零修改保持绿（自动化 `pytest` 全绿 + 无 hooks 路径回归）
- AC103: （N41）分层 + 无反向依赖——`hooks/` 包零 `rich`/`prompt_toolkit`/后端 SDK import；引擎与动作不 import `wentian.agent`/`repl`/`providers`/`tools`；`textmatch.py` 叶子纯 stdlib（自动化 import 边界断言）
- AC104: （N43）规范 + 版本——`ruff format --check .` 与 `ruff check .` 通过；零新增第三方依赖；版本号标记 v0.12（实际 semver bump 装配期交用户、spec 不擅自跨号）

以下为 v0.13 新增（子 Agent 委派）：

- AC116: （F91）统一 Agent 工具——工具注册中心**恒含且仅含一个** `Agent` 工具；其输入 schema 含 `type` / `agent_type` / `prompt` / `background` 四字段、`category=COMMAND_EXEC`；加载任意数量角色后，注册中心里 Agent 工具的数量与 schema **不随角色增减而变化**（自动化：增删角色断言暴露的工具数恒定、schema 字段齐备）。
- AC117: （F92/F93）四源加载 + 覆盖 + 容错——临时四层目录放同 `name` 角色 → 发现结果取**高层**（项目 > 用户 > 内置 > 插件，整体覆盖）；缺 `name` / 坏 frontmatter 的文件被**静默跳过**、不阻断其余、不抛异常；解析出的 `AgentDef` 各字段（name / description / body / tools / disallowed_tools / model / max_turns / permission_mode / source）正确（自动化临时目录四源 + 假内置）。
- AC118: （F93/N55）共享 frontmatter 原语——抽出的 frontmatter 解析原语**被 skills 与 agents 同时 import 调用**（断言两方走同一函数）；抽取后 **v0.11 Skill 全量测试零回归**（自动化：skills 既有测试零修改保持绿 + 新原语单测）。
- AC119: （F94）定义式跑到底 + 上下文隔离——定义式子 Agent（**假 provider**）从**空白对话**跑到 `COMPLETED`，`load_skill` 式抽取**末条助手正文**作为 Agent 工具结果返回；主会话只增「Agent 工具调用 + 其结果」一条 tool result、**不含**子 Agent 内部的多轮往返消息（自动化：对比主会话落盘 messages 与子对话内部消息，断言主会话不见中间过程）。
- AC120: （F95）Fork 式继承 + 强制后台——Fork 子 Agent 的**起始消息 = 父对话历史 + 本任务 prompt**；**强制后台**（Agent 工具立即返回「已起」占位、不阻塞）；其允许集**不含** `Agent` 工具（自动化：断言起始上下文含父历史前缀、工具立即返回、允许集无 Agent）。
- AC121: （F96/N53）状态隔离 + 独立 provider——子 Agent 用**独立 `Mode` + 独立 provider 实例**；改子 Agent 的权限模式**不影响**主 REPL 的 `_mode`；并发跑两个子 Agent → 各自结果**不串位**（自动化：断言子 Agent provider 非主 provider 同一实例、主 `_mode` 不变、两子结果各归各位）。
- AC122: （F97/N52）三层过滤 + 嵌套防护——过滤集合运算正确（白名单取**交集**、黑名单取**差集**、后台再 `∩` 免确认工具）；`Agent` 工具**不在任何子 Agent 的允许集**里；**深度 ≥ 1 再起子 Agent 被拒**（自动化：构造工具集 + 白/黑名单 + 后台标志断言结果集；断言 Agent 全局不可见；断言深度兜底拒绝）。
- AC123: （F98）三种进后台 + Fork 恒后台 + 线程不泄漏——三条进后台路径各自生效：**显式**（`background=true`）/ **超时自动**（用**假时钟**推过 `foreground_timeout_s` → 转后台、工具返「已转后台」）/ **手动切**（运行期按键推后台）；**Fork 恒后台**；管理器记录每个任务的 `status` / `result` / `usage`；daemon 线程退出时 `join` 不泄漏（自动化：假时钟 + 假 runner 断言三路径 + 线程数回归）。
- AC124: （F99）前台同步 + 后台回灌不持久化——**前台**子 Agent → Agent 工具**同步返回**结果；**后台**任务完成 → 结果写入回灌缓冲 → **下一轮** `request_decorator` `drain` 成 `<system-reminder>` 注入 outgoing messages、**不写回 messages、不持久化**（自动化：假 provider 断言前台同步返回；断言后台完成后下一轮 outgoing 含回灌文、原 messages 与落盘 messages 均不含）。
- AC125: （F100）模型别名映射——`inherit` / `haiku` / `sonnet` / `opus` → 正确模型 ID（断言 `dataclasses.replace` 后子 `ProviderConfig.model` 为映射值）；`inherit` 取**主对话当前模型**；别名**不可解析** → Agent 工具返回报错、**不空起子 Agent**（自动化：逐别名断言映射 ID + inherit 取主模型 + 非法别名报错不起）。
- AC126: （F101）`/agents` 列出 + 查看——`/agents` 无参 → 列出全部后台任务（含 id / 角色或类型 / 状态 / 相对起始时刻 / token 用量）；`/agents <id>` → 输出该任务**结果全文 + 用量**；命令走分发**零 provider 请求**（自动化：用**假管理器**驱动断言列表与单任务详情、断言 provider 零调用）。
- AC127: （F102）SubAgentAction 接通——hook 规则的 `subagent` 动作触发 → 经管理器 / runner **真起一个定义式子 Agent**（**假 provider**）落后台、结果经 F99 回灌；动作内部失败 → **只记 hook 日志、不中断主流程**（v0.12 软化铁律）（自动化：假引擎触发 subagent 动作 + 假 provider 断言子 Agent 起 + 落后台 + 回灌；断言失败软化不冒泡）。
- AC128: （N50/N51/N56）无配置回退 + 分层 + 规范——**无 `agents` 配置**（`agent_registry=None` / `task_manager=None`）时 `Agent` 工具**仍在**、`type=definition` 取不到角色**优雅报错**、`type=fork` 照常，其余行为与集成基线**等价**（仅 Agent 工具 +1 的有意新默认）；`agents/` 包**分层 import 边界**断言（`spec` / `filter` 叶子、`agents/` 对 `repl` / `cli` 零反向依赖）；`ruff format --check .` 与 `ruff check .` **双过**（自动化 `pytest` 全绿 + 无配置冒烟 + import 边界断言 + ruff 双过）。
- AC129: （端到端 🌐👁，留用户验收）——真 API key 下：真起一个**定义式**子 Agent 跑一轮任务 → 看结果回主对话、`/agents` 能列出、**上下文隔离**（主会话无子 Agent 内部往返）；真起一个**后台 / Fork** 任务 → 看**异步回灌**到主对话下一轮；尝试让子 Agent 调用 `Agent` 工具 → 看**被嵌套防护拦下**（人工真终端验收）。
