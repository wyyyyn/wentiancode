# WentianCode（文天）— spec 目录索引

> 本项目按 **mew-spec（× superpowers 合并版）** 流程开发。四份递进文档是唯一事实来源。任何改动先改文档、再动代码。
>
> **本项目约定**：四份文档**连续一次性生成、不逐份审批**，全部产出后统一审阅再进开发；开发阶段独立任务尽量开子 agent 并行（不影响质量为前提）。详见 [`../CLAUDE.md`](../CLAUDE.md)。

## 文档进度

| 阶段 | 文档 | 状态 |
|------|------|------|
| 一 · 做什么 | [`spec.md`](./spec.md) | 🟡 v0.5 已连续生成待统一审阅（F35–F40 + AC33–AC40）；v0.4 及之前已批准 |
| 二 · 怎么做 | [`plan.md`](./plan.md) | 🟡 v0.5 已连续生成待统一审阅（设计 C21–C28）；v0.4 及之前已批准 |
| 三 · 按什么顺序做 | [`task.md`](./task.md) | 🟡 v0.5 已连续生成待统一审阅（T59–T68）；v0.4 及之前已批准 |
| 四 · 做对没 | [`checklist.md`](./checklist.md) | 🟡 v0.5 验收项已连续生成待统一审阅；v0.1/v0.2 已验收，v0.3/v0.4 离线全绿（504 测试），🌐👁 项待用户验收 |

## 长期约定文档（跨版本，不随版本号滚动）

| 文档 | 管什么 |
|------|--------|
| [`teaching.md`](./teaching.md) | 每版"零基础精讲"教学页的制作规范——绑定全局《学习材料制作标准》并固化本系列专属约定（视觉系统 / 组件词汇 / 硬规则 / 五层映射 / 命名 / 截图验证）。要改教学页规范，先改本章。 |

## 三条铁律

1. 四份文档没全部生成并统一审阅通过 → 不动代码。
2. 没有先失败的测试 → 不写生产代码（TDD 红-绿-重构）。
3. 没有当场新鲜证据 → 不许声称"完成"。

## 项目一句话

yuning 自己的命令行 AI 助手（类 Claude Code）。v0.1 = 终端多轮流式对话内核（多后端 + 流式 + thinking + Markdown + 持久化）；v0.2 = Claude Code 式交互（横幅 / 后端选择 / 多行输入框 / 状态栏 / 计时反馈 / Esc 中断）；v0.3 = 工具系统（六核心工具 + 注册中心 + 双协议工具调用 + 单轮执行 + 副作用确认）；v0.4 = Agent Loop（ReAct 多轮循环 + 五停机条件 + 异步事件流解耦 + 只读并发分批 + /plan //do 计划模式）；v0.5 = 结构化系统提示 + 提示词缓存（七模块可插拔系统提示 + 文天人格 + 约定双重强化 + Anthropic cache_control 断点 + `<system-reminder>` 消息通道注入 + 缓存命中可观测）。详见 `spec.md`。
