# WentianCode（文天）— spec 目录索引

> 本项目按 **mew-spec（× superpowers 合并版）** 流程开发。四份递进文档是唯一事实来源。任何改动先改文档、再动代码。
>
> **本项目约定**：四份文档**连续一次性生成、不逐份审批**，全部产出后统一审阅再进开发；开发阶段独立任务尽量开子 agent 并行（不影响质量为前提）。详见 [`../CLAUDE.md`](../CLAUDE.md)。

## 文档进度

| 阶段 | 文档 | 状态 |
|------|------|------|
| 一 · 做什么 | [`spec.md`](./spec.md) | ✅ 已批准（2026-06-12 统一审阅通过 v0.4：F29–F34 + AC25–AC32） |
| 二 · 怎么做 | [`plan.md`](./plan.md) | ✅ 已批准（2026-06-12 统一审阅通过 v0.4 设计 C14–C20） |
| 三 · 按什么顺序做 | [`task.md`](./task.md) | ✅ 已批准（2026-06-12 统一审阅通过 T47–T58） |
| 四 · 做对没 | [`checklist.md`](./checklist.md) | ✅ 已批准（2026-06-12 统一审阅通过 v0.4 项）——v0.1/v0.2 已验收；v0.3 离线项全绿（408 测试），v0.3/v0.4 🌐👁 项待用户验收 |

## 三条铁律

1. 四份文档没全部生成并统一审阅通过 → 不动代码。
2. 没有先失败的测试 → 不写生产代码（TDD 红-绿-重构）。
3. 没有当场新鲜证据 → 不许声称"完成"。

## 项目一句话

yuning 自己的命令行 AI 助手（类 Claude Code）。v0.1 = 终端多轮流式对话内核（多后端 + 流式 + thinking + Markdown + 持久化）；v0.2 = Claude Code 式交互（横幅 / 后端选择 / 多行输入框 / 状态栏 / 计时反馈 / Esc 中断）；v0.3 = 工具系统（六核心工具 + 注册中心 + 双协议工具调用 + 单轮执行 + 副作用确认）；v0.4 = Agent Loop（ReAct 多轮循环 + 五停机条件 + 异步事件流解耦 + 只读并发分批 + /plan //do 计划模式）。详见 `spec.md`。
