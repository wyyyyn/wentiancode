# WentianCode（文天）— spec 目录索引

> 本项目按 **mew-spec（× superpowers 合并版）** 流程开发。四份递进文档是唯一事实来源。任何改动先改文档、再动代码。
>
> **本项目约定**：四份文档**连续一次性生成、不逐份审批**，全部产出后统一审阅再进开发；开发阶段独立任务尽量开子 agent 并行（不影响质量为前提）。详见 [`../CLAUDE.md`](../CLAUDE.md)。

## 文档进度

| 阶段 | 文档 | 状态 |
|------|------|------|
| 一 · 做什么 | [`spec.md`](./spec.md) | 🟡 v0.10（F70–F76 / N34–N38 / AC85–AC94：斜杠命令系统）spec 已落、待开发；✅ v0.9（F63–F69 / N28–N33 / AC75–AC84：记忆与会话）已实现；✅ v0.8 及之前已实现 |
| 二 · 怎么做 | [`plan.md`](./plan.md) | 🟡 v0.10 设计 C85–C92（命令规格 / 注册中心 / 界面控制接口 / 解析器 / 内置命令 / Tab 补全 / REPL 分发重写 / 装配）待开发；✅ v0.9 C53–C59 已实现；✅ v0.8 C47–C52 及之前已实现 |
| 三 · 按什么顺序做 | [`task.md`](./task.md) | 🟡 v0.10 任务 T108–T116（四波次：叶子原语 / 命令实现+补全 / REPL 集成 / 装配+验收）待开发；✅ v0.9 T98–T107 已完成；✅ v0.8 T90–T97 及之前已完成 |
| 四 · 做对没 | [`checklist.md`](./checklist.md) | 🟡 v0.10 checklist 已落、待开发后逐项取证；🟢 v0.9 离线全绿（2026-06-19，**1056 测试**，905→+151，离线项全部勾选、🌐👁 项待用户验收，READY FOR ACCEPTANCE）；🟢 v0.8 离线项全绿（**905 测试**，823→+82）；🟢 v0.7 离线全绿；v0.1/v0.2 已验收，v0.3–v0.9 离线全绿、🌐👁 项待用户验收 |

## 长期约定文档（跨版本，不随版本号滚动）

| 文档 | 管什么 |
|------|--------|
| [`teaching.md`](./teaching.md) | 每版"零基础精讲"教学页的制作规范——绑定全局《学习材料制作标准》并固化本系列专属约定（视觉系统 / 组件词汇 / 硬规则 / 五层映射 / 命名 / 截图验证）。要改教学页规范，先改本章。 |

## 三条铁律

1. 四份文档没全部生成并统一审阅通过 → 不动代码。
2. 没有先失败的测试 → 不写生产代码（TDD 红-绿-重构）。
3. 没有当场新鲜证据 → 不许声称"完成"。

## 项目一句话

yuning 自己的命令行 AI 助手（类 Claude Code）。v0.1 = 终端多轮流式对话内核（多后端 + 流式 + thinking + Markdown + 持久化）；v0.2 = Claude Code 式交互（横幅 / 后端选择 / 多行输入框 / 状态栏 / 计时反馈 / Esc 中断）；v0.3 = 工具系统（六核心工具 + 注册中心 + 双协议工具调用 + 单轮执行 + 副作用确认）；v0.4 = Agent Loop（ReAct 多轮循环 + 五停机条件 + 异步事件流解耦 + 只读并发分批 + /plan //do 计划模式）；v0.5 = 结构化系统提示 + 提示词缓存（七模块可插拔系统提示 + 文天人格 + 约定双重强化 + Anthropic cache_control 断点 + `<system-reminder>` 消息通道注入 + 缓存命中可观测）；v0.6 = 权限系统·五层防御（危险命令黑名单 + 路径沙箱 + 可配置规则引擎 + 三层配置合并 + 四档权限模式按类别兜底 + 人在回路三选一；五层逐层短路、判定 Allow/Deny/Ask 三态、判定门在 AgentLoop 编排层、拒绝回灌不中断、Shift+Tab 切档）；v0.7 = MCP 客户端接入（stdlib 手搓 JSON-RPC 2.0 + stdio/Streamable-HTTP 两种传输 + 同步线程客户端 id 配对 + 远端工具适配为统一工具无感调用 + 多 Server 缓存与故障隔离 + provider/mcpServers 两层配置合并；仅工具能力，不做资源/提示词/采样与健康检查/重连。**已实现，823 测试离线全绿，READY FOR ACCEPTANCE**）；v0.8 = 上下文管理 + 双层压缩（近似 token 估算锚定上次 usage + 增量字符 / 第一层超大工具结果卸载存盘留预览 / 第二层逼近窗口调 LLM 八段结构化摘要保留近期原文 / 摘要 Prompt 禁工具+先草稿后正式 / 压缩后补边界消息 / `/compact` 手动触发 + 摘要连续失败 3 次熔断 / loop 每轮请求前写回式钩子；不上精确 tokenizer、不做跨会话记忆。**已实现，905 测试离线全绿，READY FOR ACCEPTANCE**）；v0.9 = 记忆与会话（三层 `WENTIAN.md` 项目指令加载 + `@include` 内联展开限深/防环/拦越界 / 会话改 JSONL 追加写并按 cwd 分区存储 / 恢复卫生坏行跳过+尾部未配对截断+溢出复用 v0.8 Compactor 压一次+跨时间提醒 / 过期会话惰性清理 / 每轮后台 daemon 线程抽取用户偏好·纠正反馈·项目知识·参考资料四类带 frontmatter 笔记并 LLM 去重 / 记忆按用户级·项目级分级存+每域 INDEX 摘要索引启动注入「长期记忆」模块 / 新增 `memory:`·`sessions:` 两层深合并配置；填回 v0.5 留的项目指令与长期记忆两个空槽，不做向量库/RAG召回/跨机同步/会话内热刷新。**已实现，1056 测试离线全绿，READY FOR ACCEPTANCE**）；v0.10 = 斜杠命令系统（命令注册中心管元数据[名称/别名/描述/用法/类型/参数提示/隐藏/处理函数]+启动期别名冲突 panic / 解析器斜杠前缀+空格切分+命令名小写大小写不敏感+空输入早返回+未命中 /help 引导 / 三类执行模式[纯本地·影响界面状态·提示词送进对话] / `CommandContext` 界面控制接口让命令与渲染框架解耦+状态栏 `[DEFAULT]`/`[PLAN]` 模式标记 / 回车入口分流器 / 别名+Tab 补全[单匹配补·多匹配菜单·隐藏不参与] / 内置十命令 `/help`·`/clear`·`/compact`·`/plan`·`/do`·`/session`·`/memory`·`/permission`·`/status`·`/review` 并归并老命令[`/new`/`/sessions`/`/resume`→`/session` 子命令、`/provider`/`/exit` 独立登记]；不做用户自定义命令/动态生成提示词/命令级权限控制[留 Skill 系统]。**spec 已落、待开发**）。详见 `spec.md`。
