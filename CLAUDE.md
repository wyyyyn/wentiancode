# WentianCode（文天）— 开发约定

> 本项目 **spec 驱动**，`spec/` 是唯一事实来源；任何改动先改 spec 文件、再动代码。详见 [`spec/README.md`](./spec/README.md)。

## 开发流程

1. **spec 优先**：`spec.md → plan.md → task.md → checklist.md` 四份文档是所有功能的唯一事实来源。**先改文档，再动代码**；spec 与实现冲突时，以 spec 为准。
2. **能并行就开子 agent**：独立、无共享状态/无顺序依赖的任务，优先并行执行（每个只负责一件事，任务指令里带上相关 spec 路径/内容）。
3. **有依赖则串行**：改同一批文件、或并行会降低正确性的，仍串行做。

## 不可妥协的底线

- **TDD 红-绿-重构**：没有先失败的测试 → 不写生产代码。
- **完成前验证**：没有当场新鲜证据（跑测试 / PTY 验证 / 截图）→ 不许声称"完成"。
- **spec 优先**：spec 与实现冲突，以 spec 为准；要变方向先回去改 spec。
