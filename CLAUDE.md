# WentianCode（文天）— 项目工作约定

> yuning 自己的命令行 AI 助手（类 Claude Code）。本项目 **spec 驱动**，`spec/` 是唯一事实来源；任何改动先改 spec 文件、再动代码。详见 [`spec/README.md`](./spec/README.md)。

## 开发流程约定（本项目专属，覆盖全局默认）

1. **四份 spec 文档连续生成 → 自动进开发**：`spec.md → plan.md → task.md → checklist.md` 一次性连续产出，**不逐份停下来审批**；**四份生成完毕即自动进入开发，不停统一审阅关卡**（用户 2026-06-16 拍定）。仍遵守"先 plan mode 出方向、再落 spec、最后动代码"的顺序铁律——变的只是文档生成后不再设人工审批关卡。
   - 用户介入点只剩两个：**脑暴** 与 **验收**；中间（spec 生成、开发实现）全自动跑。
   - 用户若当轮明确要求"先审/等我看"则尊重；否则默认文档好就开工。
2. **能并行就开子 agent**：开发阶段，相互独立、无共享状态/无顺序依赖的任务，优先派子 agent 并行执行——**前提是不影响开发质量**。
   - 每个子 agent 只负责一件事，且在任务指令里带上相关 spec 路径/内容对齐。
   - 有依赖、会改同一批文件、或并行会降低正确性的，仍串行做。

## 不可妥协的底线（并行/连续不豁免）

- **TDD 红-绿-重构**：没有先失败的测试 → 不写生产代码。
- **完成前验证**：没有当场新鲜证据（跑测试 / PTY 验证 / 截图）→ 不许声称"完成"。
- **spec 优先**：spec 与实现冲突，以 spec 为准；要变方向先回去改 spec。
