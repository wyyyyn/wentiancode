[project]
name = "wentian"
version = "0.1.0"
description = "A terminal AI assistant REPL"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "openai",
    "typer",
    "rich",
    "pyyaml",
]

[project.scripts]
wentian = "wentian.cli:app"
wt = "wentian.cli:app"

[dependency-groups]
dev = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wentian"]
